# プロジェクト前提 — ゲーミングPC fps予想ツール＋スペックDB

> このファイルは新しいセッションの最初に必ず読むこと。
> ここに書かれた判断は検討済みの結論であり、勝手に変更しないこと。
> 変更が必要だと判断した場合は、実装前にユーザーに確認すること。

## プロジェクトの狙い

ゲーミングPC構成（GPU・CPU）と、ゲーム・解像度・画質設定を入力すると、
予想fps（平均・1% Low・高負荷時）を返すツール。
併せてGPU/CPUのスペックデータベースを持ち、そこからSEO流入を得る。

収益化はBTOパソコン・グラフィックボード・光回線などのアフィリエイト。
診断結果の出口がそのまま案件になる導線を前提に設計している。

---

## 絶対に守るルール

### 1. 他社のベンチマーク数値を転載しない

ドスパラ、各種レビューサイト、YouTube等が公開しているベンチマーク数値を、
体系的に収集して自前のデータベースにすることは行わない。

理由は3つ:

- **法的リスク**: 個々の数値は事実だが、表やデータベース全体は「データベースの著作物」
  として保護される（著作権法12条の2）。出典明記でも転載は正当化されない。
  引用（32条）も、データが機能の中核になると主従関係が逆転して成立しない。
  加えて多くのサイトは利用規約でスクレイピング・再利用を禁止しており、
  これは著作権とは別に契約違反になる。
- **品質**: 投稿者ごとにテストシーン・設定・ドライバ・組み合わせるパーツが異なり、
  条件の違うデータを混ぜると指数がノイズだらけになる。
- **SEO**: 他所の数値の寄せ集めは付加価値ゼロの重複コンテンツと判定される。
  「劣化版〇〇」にしかならない。

**使ってよいもの**: メーカー公式の公開スペック（コア数、クロック、バス幅、
キャッシュ容量、TDP等）、メーカー公表のIPC向上率、自前の実測、ユーザー投稿。

**公開ベンチの正しい使い道**: 検算。自前モデルの予測値が
レビューサイトの実測と何%ズレているかを確認する用途。

---

#### 例外: 係数だけは第三者の測定から取ってよい（2026-08-30 変更・ユーザー承認済み）

**変更の理由**: Valorant 以外のタイトル（Cyberpunk 2077 / CS2 / Fortnite /
Apex Legends）を自分で測定できないため。測定できないと対応タイトルが増えず、
中核機能であるfps予想ツールが1タイトルのまま止まる。

##### 取ってよいもの / だめなもの

| | 扱い |
|---|---|
| 解像度係数（4K→1440p など） | **取ってよい** |
| 設定係数（最高→中 など） | **取ってよい** |
| ゲーム間の重さの比（Valorant を1とした倍率） | **取ってよい** |
| ゲーム固有のfps上限 | 事実。ゲーム内で自分で確認する |
| **モデル別のfps数値表** | **禁止。従来どおり** |
| **特定GPUの「〇〇で△△fps」という値の保存** | **禁止** |

つまり保存するのは**割り算の結果だけ**。1タイトルあたり5〜7個の数字で済む。
モデル別のスケーリングは引き続き自前の指数（メーカー公式スペックから計算）が担う。

##### なぜこれなら許容できるか（3つの理由への対応）

- **法的リスク**: 係数からは元の数値表を復元できず、ソースと競合しない。
  取得量が表全体ではなく数個の派生値に留まる。
  **可能なら測定者から許諾を取る**（クレジット表記とリンクを条件に応じてもらえることがある）
- **品質**: 比率は同一構成・同一テストシーンの中でしか意味を持たないため、
  **1つのソース内で条件だけが変わっている測定に限定する**。
  複数の独立したソースで同じ係数を検算し、大きくばらつくなら採用しない
- **SEO**: 数値表を持たないので重複コンテンツにならない。
  自前の指数と組み合わせた予測は独自のもの

##### ソースの条件（1つでも欠けたら使わない）

1. 同一のPC構成で、条件だけを変えた複数の測定が1つのソースに含まれる
2. 構成が明記（GPU / CPU / メモリ / ドライバ版）
3. 設定が明記（プリセット名、**アップスケーリングの有無と品質モード**、レイトレの有無）
4. テストシーンが明記（内蔵ベンチマーク使用が最良）
5. 平均fpsが数値で読める（グラフの目視読み取りは使わない）

**読むときの落とし穴**:

- **低解像度でCPU律速に入っていると解像度係数は取れない**。数値が頭打ちで
  平らになっていたらそれ。Valorant の1080pでまさにこれが起きている
- **Apex は300fps上限**。300に張り付いた測定からは係数が取れない
- アップスケーリングの設定が条件間で揃っていないものは使わない

##### 記録すること（採用したソースごとに、下の「ゲーム別の係数」に残す）

出典URL / 公開日 / 測定構成 / 各条件の設定 / **原文の数値** / 算出した係数 /
許諾の有無。後から検算できない形では採用しない。

##### サイト文言の修正（第三者由来の係数を初めて入れる時点で必須）

現在サイトの4か所に「他社のベンチマーク数値は使っていません」と書いてある
（`grep -rn "他社のベンチマーク数値" src/` で確認できる）。

- `src/app/layout.tsx` のフッター
- `src/app/page.tsx` のトップページ
- `src/components/EstimateNote.tsx`（GPU/CPU一覧の注記）
- `src/app/tools/fps/page.tsx`

**係数を1つでも取り込んだ時点で、この4か所は事実と食い違う。**
「ゲーム別の係数は公開されている第三者の測定から算出、モデル別の値は
メーカー公式スペックから自前で計算」という趣旨に書き換えること。
出典一覧のページも用意する。

##### ゲーム別の係数

| ゲーム | 解像度指数 k | 設定係数（4K） | Valorant比 | 出典 | 許諾 |
|---|---|---|---|---|---|
| VALORANT | 0.772（実測1.87から導出） | 高→低 x1.74 | GPU 1.00 / CPU 1.00 | **自前の実測** | — |
| Fortnite | Perf 0.464 / 低 0.464 / 中 0.471 / Epic 0.639 | Epic基準で 中x5.20 / 低x7.875 / Perf x13.125 | GPU 0.0856 / CPU 0.671 | Boss Benchmarks | 取得済み（詳細未記入） |

#### Fortnite の係数の算出過程（2026-08-30）

**出典**: Boss Benchmarks「RTX 5070 Ti | Fortnite | 1080p, 1440p, 4K」
https://www.youtube.com/watch?v=A5ACNelDcRU
**測定構成**: Ryzen 7 9800X3D（自前の実測機と同一CPU）/ ASUS TUF RTX 5070 Ti /
64GB DDR5 / Windows 11 Pro。動画のオーバーレイから AVG・1%・GPU使用率・VRAM を読み取った。
**許諾**: 取得済み。「動画の情報はすべて使ってよい。質問や依頼には応じられない」。
※ 取得日と条件の詳細は未記入。分かり次第埋めること。

読み取った15条件（AVG / 1% / GPU% / VRAM MB）:

```
1080p  Perf 593/155/45/2482   Low 504/266/79/3501   Med 400/171/84/4416   High 292/165/96/4568   Epic  96/57/95/7619
1440p  Perf 651/170/81/2316   Low 459/221/88/4174   Med 301/163/89/5020   High 215/131/93/4849   Epic  73/49/92/8425
4K     Perf 525/145/98/2779   Low 315/193/98/4256   Med 208/125/97/5124   High  63/44/97/9018   Epic  40/30/97/9981
```

**High は不採用**。4Kだけ Lumen がオン（fpsが1440pの3.4倍遅く、VRAMも4849→9018と倍増）。
1080p/1440p はオフで、解像度比較が成立しない。作者に確認できなかったため数値から判断した。

**解像度指数** は GPU律速の点（GPU使用率が高い条件）だけを使い、
`fps ∝ 画素数^(-k)` を最小二乗でフィットした。プリセットごとに k が違うのは、
重い設定ほど画素あたりの処理が支配的になるため。

**ゲーム間の重さ**:
- GPU側 = 4K Epic 40fps ÷ RTX 5070 Ti の Valorant 4K高 467.5 = 0.0856
- CPU側 = 観測最大 651fps ÷ Valorant CPU天井 970.3 = 0.671
  ※ 651 は GPU81% で飽和していないため「651以上」としか言えない。保守的に651を採用。

**モデルの当てはまり**（12条件中10条件が±10%以内）:
1080p/低 が +18.9% ずれる。GPU79%・CPU57%の混在領域で、`min()` では表せない既知の制約。

**シーンのばらつきは約10%**。1080p Perf(593) < 1440p Perf(651) と逆転していることから分かる。
実際のプレイ映像なのでテストシーンを固定できていない。

**「クリエイティブでは33%程度上回る」はユーザー提供の知見**（測定ではない）。
公開マッチでの計測である旨とあわせて、ツールの画面に表示している。

#### 評価したが不採用にしたソース

| ソース | 判定 | 理由 |
|---|---|---|
| Digital Trends（Fortnite） | 不採用 | 1080p/1440pともCPU律速。指数0.133で解像度がほぼ効いていない。4Kなし |
| ComputerBase（Fortnite） | 不採用 | 作りは最良だが、アップスケーリングが解像度ごとに変わる（1080p Quality / 1440p Balanced / 4K Performance） |
| fpsbench.com | 除外 | 測定値ではなく集約・推定。CPU×GPUの全組み合わせにページがある。TechPowerUp等の集約元の権利も及ばない |

**再利用できる判定基準**:

1. **解像度ごとにアップスケーリングのモードが変わるソースは使えない**。
   解像度の重さとアップスケーリングの効きが混ざり、解像度係数が取れない
2. **CPU×GPUの任意の組み合わせにページがあるサイトは計算機**であって測定サイトではない。
   実測は組み合わせの数だけ手間がかかるので、網羅は物理的に不可能

### 2. 数値をLLMの記憶から出させない

fps値でもスペック値でも、モデルの記憶から「それっぽい数字」を生成させない。
必ず一次ソース（メーカー公式ページ）で確認するか、計算式から導出する。
ゲーマーは数値にシビアで、外れた推定を断定口調で出すと信用を失う。

AIの使いどころは「計算式の設計」と「結果の解説文生成」であって、
「数値そのものの生成」ではない。

### 3. 推定値であることを常に明示する

予想fpsも性能指数も「推定値・誤差±15〜20%」と明記する。
断定的な数値表示はしない。算出方法も公開する。

これは弱みの告白ではなく差別化になる。他サイトは3DMarkスコアを
載せるだけで、それが実ゲームfpsとどう対応するかを説明していない。

### 4. AI推論をユーザー向け機能で多用しない

使われるほど赤字になる。計算・診断・変換はブラウザ内で完結させる。
解説文をLLMで生成する場合は「GPU+CPU+ゲーム+設定」の組み合わせ単位で
キャッシュし、同一条件では再生成しない。

---

## データファイル

| ファイル | 内容 |
|---|---|
| `gpu_specs_draft.csv` | GPU 78件の検証済みスペック（GTX 10〜RTX 50、RX 5000〜RX 9000） |
| `cpu_specs_draft.csv` | CPU 42件の検証済みスペック（Ryzen 5000〜9000、Intel 10〜14世代＋Core Ultra 200S） |
| `gpu_index.csv` | GPU性能指数（算出結果） |
| `cpu_index.csv` | CPU性能指数（算出結果） |
| `make_index.py` | 指数の計算スクリプト。係数を変えて再計算できる |
| `fps_measurement_sheet.csv` | 実測データと導出された係数 |
| `benchmark-list.html` | 一覧ページのプロトタイプ（単一HTML、動作する） |

### スペックデータの検証状況

全120件をメーカー公式資料と照合済み。`verified` 列に状態が入っている。
検証の過程で見つかった誤りは3件（すべてGPUのメモリ周り）:

- RX 5600 XT: メモリ14→12Gbps、帯域336→288GB/s
- RX 5600 XT: ブースト1560→1620MHz
- RX 9070: ブースト2520→2540MHz

CPUは42件すべて誤りゼロだった。

**クロックはすべてリファレンス仕様値で統一している**。
NVIDIAはFounders Editionが別クロックのモデルがある（例: RTX 2070は
リファレンス1620MHz / FE 1710MHz、TDPも175W / 185W）。
混在させないこと。

**ソース間で数値が割れているもの**（Intel公式値を採用）:
- i7-13700K ブースト: Intel公式5.4GHz / 一部メディア5.3GHz
- i5-12600K Pコアベース: Intel公式3.7GHz / 一部メディア3.6GHz

---

## 実測データ（唯一のアンカー）

2026-08-29 測定。構成は以下の1台のみ。

```
GPU     Radeon RX 9070 XT 16GB (ASUS)
CPU     Ryzen 7 9800X3D
RAM     32GB (16GB x2) DDR5
Board   MSI B650 GAMING PLUS WIFI
Monitor フルHD (1920x1080) ※高解像度はAMD VSRで描画
計測    CapFrameX 1.8.6 / 120秒キャプチャ / Remove outliers ON
環境    Resizable BAR: ON / HAGS: ON / Win Game Mode: ON
```

### Valorant 実測4点

| 条件 | 解像度 | 設定 | avg | 1% Low | 0.2% Low | GPU使用率 | 律速 |
|---|---|---|---|---|---|---|---|
| A | 1920x1080 | 全て低 | 970.3 | 676.5 | 605.5 | 57% | CPU |
| B | 3840x2160 | 全て高 | 464.6 | 387.1 | 374.6 | 99% | GPU（完全） |
| C | 2560x1440 | 全て高 | 868.8 | 653.7 | 598.2 | 97% | 混在 |
| D | 3840x2160 | 全て低 | 807.5 | 611.8 | 566.6 | 99% | 混在 |

### 導出された係数

```
CPU天井（Valorant / 9800X3D）  = 970.3 fps
GPU基準点（4K高 / 9070 XT）    = 464.6 fps
解像度係数（4K → 1440p）       = x1.87   ※ピクセル比の理論値は2.25
設定係数（高 → 低 / 4K）        = x1.74
```

解像度係数が理論値2.25より小さいのは、UI描画やCPU処理など
解像度に依存しない部分があるため。実勢としてはこれが正しい傾向。
ただし条件CはGPU Limit Time 85%で純粋なGPU律速ではないため、
この1.87はやや過小評価の可能性がある。

### 1080p の係数（実測なし・外挿）

**2026-08-30 追加。ここは根拠が最も弱い。**

1080pは最も使われる解像度だが、実測の1080pは条件A（全て低）だけで、
そこはCPU律速だったためGPU側の係数が取れていない。
そこで実測の1.87と画素比2.25から指数則の指数を求めて外挿している。

```
1.87 = 2.25^k              →  k = ln(1.87)/ln(2.25) = 0.772
1440p→1080p の画素比        =  (2560x1440)/(1920x1080) = 1.778
1440p→1080p の係数          =  1.778^0.772 = 1.56
```

計算式からの導出であり、記憶から出した数字ではない（絶対ルール2）。
**ただし実測ではないので、ツールの画面上で外挿である旨を明示すること。**
1080pの実測が取れたら、この値を差し替える。

設定係数1.74も4Kでの実測値であり、他の解像度でも同じとみなしている。
これも仮定なので明示する。

---

## 予想fpsの式

```
予想fps = min(GPU由来のfps, CPU由来のfps, ゲーム固有の上限)
```

低解像度ではCPU項が効き、高解像度ではGPU項が効く。
この構造は実測で検証済み。係数から「1440p低設定」を計算すると
464.6 x 1.87 x 1.74 = 1510fps になるが、CPU天井970.3で頭打ちになる。
これは実測の条件A（1080p低で970.3、GPU使用率57%）と整合する。

### ゲーム固有の上限

| ゲーム | 上限 | 備考 |
|---|---|---|
| Apex Legends | 300 fps | エンジン仕様。`+fps_max 0` でも解除不可 |
| Valorant | なし | 設定で上限解除できる |

対応タイトルを増やすときは、毎回この上限を確認すること。
明記しているサイトは少ないので差別化情報になる。

### v1のツールで出さない値（2026-08-30）

根拠が足りないものは**出さない**。画面に「なぜ出さないか」を書く。

- **1% Low** — 実測4点の平均fps比が 0.70 / 0.83 / 0.75 / 0.76 とばらつき、
  固定比では出せない。最低の0.70はCPU律速の条件A、最高の0.83は完全GPU律速の
  条件B。律速側で落ち方が変わる可能性があり、投稿データで検証してから出す
- **高負荷時（終盤）の平均fps** — 実測データが存在しない。
  120秒キャプチャに該当する区分が無く、測定方法の定義から必要
- **Valorant以外のタイトル** — 係数がゼロ。Apexは300fps上限のため測定不能

出せるのは「Valorant・低/高プリセット・平均fps」と、
**律速の判定（ボトルネック診断）**まで。診断は min() のどれが効くかで
構造的に決まるため、係数が1タイトル分でも成立する。

---

## 性能指数の算出方法

`make_index.py` に実装済み。入力は公開スペックとメーカー公表IPCのみ。

### GPU

```
compute    = シェーダー数 x アーキ効率係数 x ブーストクロック(GHz)
cache_mult = 1 + min(InfinityCache, 128) / 128 * 0.60
mem        = 帯域幅 x cache_mult
index      = compute^0.78 x mem^0.22        → RX 9070 XT = 100 に正規化
```

アーキ効率係数（重要）:

```
Pascal 1.00 / Turing 1.15 / Ampere 0.66 / Ada 0.78 / Blackwell 0.82
RDNA 0.95 / RDNA2 1.05 / RDNA3 1.08 / RDNA4 1.50
```

**NVIDIAのAmpere以降はCUDAコアがFP32倍カウント表記**のため係数を下げている。
AMDはRDNA全世代で「CU x 64」の一貫表記なので倍カウント補正は不要。
※ここを取り違えるとRX 6950 XTが7900 XTXより上位に来る（実際に一度やらかした）。

RDNA4が1.50と突出しているのは、64CUの9070 XTが前世代96CUの7900 XTXと
拮抗する事実を反映したもの。

### CPU

```
st        = メーカー公表IPC x ブーストクロック
cache_f   = 1 + 0.22 * ln(L3 / 32)
core_f    = 0.80 + 0.20 * min(コア数, 8) / 8      ※8コアで飽和
index     = st x cache_f x core_f x penalty       → 9800X3D = 100 に正規化
```

IPC（Zen3 = 1.00 基準、メーカー公表値ベース）:

```
Zen 3 1.00 / Zen 4 1.13 / Zen 5 1.31
Comet Lake 0.82 / Rocket Lake 0.95 / Alder Lake 1.10
Raptor Lake 1.12 / Raptor Lake-R 1.12 / Arrow Lake 1.19
```

**デュアルCCDの扱い（重要）**:

- 非X3Dで9コア以上のRyzen: L3は32MBx2に分割される。ゲームは片方のCCDで
  動くため実効L3は半分。※これを見落とすと9950Xが9800X3Dより上位に来る
- X3Dで9コア以上: 実効L3は96MB（キャッシュ側CCDの容量）。
  実効クロックもキャッシュ側CCDの低い方になるため boost - 0.5GHz で近似
- 12コアX3D（9900X3D / 7900X3D）: キャッシュ側CCDが6コアしかないため
  penalty 0.93 を追加

### この指数の限界（必ず表示すること）

- 実測は9070 XT + 9800X3Dの**1構成のみ**。他モデルは理論値からの外挿
- 誤差は±15〜20%程度
- **近接したモデル同士の順位は信用できない**。RTX 4060とRX 7600のような
  数%差の組み合わせは逆転しうる。アーキごとに係数を1つしか持てず、
  同じアーキでも上位ダイと下位ダイでキャッシュ量が違うため
- 世代をまたいだ大きな差はそれなりに信用できる
- Intel Arcは合成ベンチのスコアほど実ゲーム性能が出ない傾向があり、
  この方式では特に外れる可能性が高い（現在データ未収録）

---

## VRAMについて

**平均fpsにはほとんど影響しないが、1% Lowには強く効く。**
VRAM不足はカクつきの主因なので、独立した判定軸として持つこと。

実測で得られた基準（Valorant 4K）:

| 条件 | VRAM使用量 |
|---|---|
| 4K 全て高 | 8.19 GB |
| 4K 全て低 | 8.41 GB |

**画質を下げてもVRAMは減らなかった**（むしろ増えた）。
「設定を下げればVRAM不足が解消する」という案内は誤りになりうる。

4K高設定で8.19GB使うため、**8GBカード（RTX 4060、RX 7600等）では不足する**。
16GBの検証機では8GBカードのカクつきを再現できないため、
この領域はユーザー投稿で埋める必要がある。

---

## ユーザー投稿機能の設計要件

実測データを集める仕組みが、このサイト唯一の模倣不可能な資産になる。
公開ベンチが手薄な「設定別fps」「1% Low」「終盤高負荷時」こそ価値がある。

投稿フォームに必須で入れる項目:

- **CapFrameXの Remove outliers をONにした値を送ってもらう**
  ※これを指定しないと投稿者ごとに0.1% Lowが10倍以上ばらつく
- 計測前のウォームアップ（5分程度のプレイ）を指示する
  ※冷えた状態だとブーストが高めに出る。また起動直後はシェーダー
  コンパイルで巨大なスパイクが出て0.1% Lowが壊れる
- GPU使用率ではなく **GPU Limit Time** で律速判定する
  ※使用率97%でもGPU Limit Time 85%＝純粋なGPU律速ではないケースがある。
  使用率だけ見ていると見抜けない
- 解像度・画質設定・アップスケーリングの有無・ドライバ版・
  Resizable BAR / HAGS の状態

---

## 実装状況と次の作業

### できているもの

- スペックDB（GPU 78 / CPU 42、全件検証済み）
- 性能指数の算出ロジック
- 一覧ページのプロトタイプ（`benchmark-list.html`、単一HTMLで動作）
  - GPU/CPUタブ、検索、メーカー・アーキ絞り込み、全列ソート
  - 行タップで詳細展開、最大3モデルの比較
  - ソート中の指標が行の背景バーとして可視化される
  - モバイルではカード表示

### 次にやること（優先順）

1. **一覧ページに指数の列を追加**
2. **個別モデルページ**（120モデル分）。SEOの本命はここ。
   「RTX 5070 スペック」等の検索を拾う入口が120本できる。
   ただしスペック表だけだと薄いコンテンツ判定のリスクがあるので、
   同世代・同価格帯との違いなど固有情報を足す必要がある
3. **ユーザー投稿機能**
4. **fps予想ツール本体**

> **2026-08-30 変更（ユーザー承認済み）**: 3と4を入れ替えた。
> 実測データが集まるにはカレンダー時間がかかるため、収集の口を先に開けないと
> 予想ツールに着手する時点でデータが1台分のまま増えていない。
> 下の「公開の順番についての判断」（投稿を集めてから予想ツールを完成させる）と
> 元の並びが食い違っていたのを、公開の順番側に揃えた形。
> 実装単位への分解は `ROADMAP.md` を参照。

### 公開の順番についての判断

> **2026-08-30 変更（ユーザー承認済み）。以前の判断は下に残す。**
>
> **fps予想ツールを一覧ページの次に作る。** 個別モデルページと投稿機能より先。
>
> 理由: スペックDBだけでは他サイトと差別化できず、訪問理由にならない。
> ツールがサイトの訪問理由になる。サイトは複数のツールとゲーマー向け記事を持つ
> 収益サイトとして設計する方針になった。
>
> **精度の懸念（下の旧判断）は、対応範囲を絞ることで担保する。**
> ボトルネック診断を主役にし、fpsの絶対値は実測の裏付けがある範囲
> （Valorant・低/高プリセット・平均fpsのみ）に限定する。
> 出さない値と理由は「v1のツールで出さない値」に記載。
> 律速の判定は min() の構造から決まるので、係数が1タイトル分でも成立する。

**旧判断（2026-08-29）:**

一覧ページを先に出して集客し、来訪者から実測投稿を集めて
予想ツールを後から完成させる。

予想ツールを先に出すと、精度が低い状態で公開することになり、
数値にシビアなゲーマー相手だと信用を失う。

この懸念自体は今も有効。**だから範囲を絞る**という形で対応している。
範囲を広げるとき（対応ゲームを増やす等）は、必ず実測を先に取ること。

---

## サイトに載せられる独自の発見

実測の過程で得られた、他サイトにない情報。

- Apexは300fps上限（エンジン仕様、解除不可）
- **RX 9070 XT + Ryzen 7 9800X3D は、Apexでは1440p最高設定でも
  4K最低設定でも300上限に張り付く**＝この構成はApexにはオーバースペック。
  「設定を下げても無駄」を教えてくれるツールは少ない
- Valorantの解像度スケーリングは実測1.87倍（理論値2.25倍より緩やか）
- Valorant 4K高設定でVRAM 8.19GB＝8GBカードでは不足
- 画質を下げてもVRAMは減らない
- GPU使用率97%でもGPU Limit Time 85%＝純粋なGPU律速ではない

---

## 技術スタック

- Next.js / TypeScript / Tailwind CSS / Supabase / Vercel
- Claude Code（VS Code拡張）で実装、手動承認モードを使用
- 複雑なタスクではプランモードを使う
