# プロジェクト前提 — ゲーミングPC fps予想ツール＋スペックDB

> このファイルは新しいセッションの最初に必ず読むこと。
> ここに書かれた判断は検討済みの結論であり、勝手に変更しないこと。
> 変更が必要だと判断した場合は、実装前にユーザーに確認すること。

## プロジェクトの狙い

ゲーミングPC構成（GPU・CPU）と、ゲーム・解像度・画質設定を入力すると、
予想fps（平均・1% Low・高負荷時）を返すツール。
併せてGPU/CPUのスペックデータベースを持ち、そこからSEO流入を得る。

収益化はBTOパソコン・グラフィックボード・光回線などのアフィリエイト。
診断結果の出口がそのまま案件になる導線を前提に設計している。

---

## 絶対に守るルール

### 1. 他社のベンチマーク数値を転載しない

ドスパラ、各種レビューサイト、YouTube等が公開しているベンチマーク数値を、
体系的に収集して自前のデータベースにすることは行わない。

理由は3つ:

- **法的リスク**: 個々の数値は事実だが、表やデータベース全体は「データベースの著作物」
  として保護される（著作権法12条の2）。出典明記でも転載は正当化されない。
  引用（32条）も、データが機能の中核になると主従関係が逆転して成立しない。
  加えて多くのサイトは利用規約でスクレイピング・再利用を禁止しており、
  これは著作権とは別に契約違反になる。
- **品質**: 投稿者ごとにテストシーン・設定・ドライバ・組み合わせるパーツが異なり、
  条件の違うデータを混ぜると指数がノイズだらけになる。
- **SEO**: 他所の数値の寄せ集めは付加価値ゼロの重複コンテンツと判定される。
  「劣化版〇〇」にしかならない。

**使ってよいもの**: メーカー公式の公開スペック（コア数、クロック、バス幅、
キャッシュ容量、TDP等）、メーカー公表のIPC向上率、自前の実測、ユーザー投稿。

**公開ベンチの正しい使い道**: 検算。自前モデルの予測値が
レビューサイトの実測と何%ズレているかを確認する用途。

---

#### 例外: 係数だけは第三者の測定から取ってよい（2026-08-30 変更・ユーザー承認済み）

**変更の理由**: Valorant 以外のタイトル（Cyberpunk 2077 / CS2 / Fortnite /
Apex Legends）を自分で測定できないため。測定できないと対応タイトルが増えず、
中核機能であるfps予想ツールが1タイトルのまま止まる。

##### 取ってよいもの / だめなもの

| | 扱い |
|---|---|
| 解像度係数（4K→1440p など） | **取ってよい** |
| 設定係数（最高→中 など） | **取ってよい** |
| ゲーム間の重さの比（Valorant を1とした倍率） | **取ってよい** |
| 1% Low ÷ 平均fps | **取ってよい**（2026-09-09 追加） |
| GPU性能の効き方（性能指数が2倍のときfpsが何倍か、の指数） | **取ってよい**（2026-09-17 追加。GPUだけを替えた測定も「条件だけを変えた」に含む） |
| 場面の重さ（同じ構成で場所だけを変えた測定の比） | **取ってよい**（2026-09-17 追加） |
| ゲーム固有のfps上限 | 事実。ゲーム内で自分で確認する |
| **モデル別のfps数値表** | **禁止。従来どおり** |
| **特定GPUの「〇〇で△△fps」という値の保存** | **禁止** |

つまり保存するのは**割り算の結果だけ**。1タイトルあたり5〜7個の数字で済む。
モデル別のスケーリングは引き続き自前の指数（メーカー公式スペックから計算）が担う。

##### なぜこれなら許容できるか（3つの理由への対応）

- **法的リスク**: 係数からは元の数値表を復元できず、ソースと競合しない。
  取得量が表全体ではなく数個の派生値に留まる。
  **可能なら測定者から許諾を取る**（クレジット表記とリンクを条件に応じてもらえることがある）
- **品質**: 比率は同一構成・同一テストシーンの中でしか意味を持たないため、
  **1つのソース内で条件だけが変わっている測定に限定する**。
  複数の独立したソースで同じ係数を検算し、大きくばらつくなら採用しない
- **SEO**: 数値表を持たないので重複コンテンツにならない。
  自前の指数と組み合わせた予測は独自のもの

##### ソースの条件（1つでも欠けたら使わない）

1. 同一のPC構成で、条件だけを変えた複数の測定が1つのソースに含まれる
2. 構成が明記（GPU / CPU / メモリ / ドライバ版）
3. 設定が明記（プリセット名、**アップスケーリングの有無と品質モード**、レイトレの有無）
4. テストシーンが明記（内蔵ベンチマーク使用が最良）
5. 平均fpsが数値で読める（グラフの目視読み取りは使わない）

**読むときの落とし穴**:

- **低解像度でCPU律速に入っていると解像度係数は取れない**。数値が頭打ちで
  平らになっていたらそれ。Valorant の1080pでまさにこれが起きている
- **Apex は300fps上限**。300に張り付いた測定からは係数が取れない
- アップスケーリングの設定が条件間で揃っていないものは使わない

##### 記録すること（採用したソースごとに、下の「ゲーム別の係数」に残す）

出典URL / 公開日 / 測定構成 / 各条件の設定 / **原文の数値** / 算出した係数 /
許諾の有無。後から検算できない形では採用しない。

##### サイト文言の修正（第三者由来の係数を初めて入れる時点で必須）

現在サイトの4か所に「他社のベンチマーク数値は使っていません」と書いてある
（`grep -rn "他社のベンチマーク数値" src/` で確認できる）。

- `src/app/layout.tsx` のフッター
- `src/app/page.tsx` のトップページ
- `src/components/EstimateNote.tsx`（GPU/CPU一覧の注記）
- `src/app/tools/fps/page.tsx`

**係数を1つでも取り込んだ時点で、この4か所は事実と食い違う。**
「ゲーム別の係数は公開されている第三者の測定から算出、モデル別の値は
メーカー公式スペックから自前で計算」という趣旨に書き換えること。
出典一覧のページも用意する。

##### ゲーム別の係数

| ゲーム | 解像度指数 k | 設定係数（4K） | Valorant比 | GPU性能の効き方 | 出典 | 許諾 |
|---|---|---|---|---|---|---|
| VALORANT | 0.772（実測1.87から導出） | 高→低 x1.74 | GPU 1.00 / CPU 1.00 | 1（定義） | **自前の実測** | — |
| Fortnite | Perf 0.464 / 低 0.464 / 中 0.471 / Epic 0.639 | Epic基準で 中x5.20 / 低x7.875 / Perf x13.125 | GPU 0.0856 / CPU 0.671 | 1（測定GPUが1枚で確かめられない） | Boss Benchmarks | 取得済み（詳細未記入） |
| Apex Legends | 全プリセット 0.587（低・中は最高から流用） | 最高基準で 中x1.21 / 低x1.28（1080pで算出） | GPU 0.267（指数100のGPUで）/ CPU 0.351 | **0.665** | ちもろぐ | 取得済み（出典表記は任意） |

#### Fortnite の係数の算出過程（2026-08-30）

**出典**: Boss Benchmarks「RTX 5070 Ti | Fortnite | 1080p, 1440p, 4K」
https://www.youtube.com/watch?v=A5ACNelDcRU
**測定構成**: Ryzen 7 9800X3D（自前の実測機と同一CPU）/ ASUS TUF RTX 5070 Ti /
64GB DDR5 / Windows 11 Pro。動画のオーバーレイから AVG・1%・GPU使用率・VRAM を読み取った。
**許諾**: 取得済み。「動画の情報はすべて使ってよい。質問や依頼には応じられない」。
※ 取得日と条件の詳細は未記入。分かり次第埋めること。

読み取った15条件（AVG / 1% / GPU% / VRAM MB）:

```
1080p  Perf 593/155/45/2482   Low 504/266/79/3501   Med 400/171/84/4416   High 292/165/96/4568   Epic  96/57/95/7619
1440p  Perf 651/170/81/2316   Low 459/221/88/4174   Med 301/163/89/5020   High 215/131/93/4849   Epic  73/49/92/8425
4K     Perf 525/145/98/2779   Low 315/193/98/4256   Med 208/125/97/5124   High  63/44/97/9018   Epic  40/30/97/9981
```

**High は不採用**。4Kだけ Lumen がオン（fpsが1440pの3.4倍遅く、VRAMも4849→9018と倍増）。
1080p/1440p はオフで、解像度比較が成立しない。作者に確認できなかったため数値から判断した。

**解像度指数** は GPU律速の点（GPU使用率が高い条件）だけを使い、
`fps ∝ 画素数^(-k)` を最小二乗でフィットした。プリセットごとに k が違うのは、
重い設定ほど画素あたりの処理が支配的になるため。

**ゲーム間の重さ**:
- GPU側 = 4K Epic 40fps ÷ RTX 5070 Ti の Valorant 4K高 467.5 = 0.0856
- CPU側 = 観測最大 651fps ÷ Valorant CPU天井 970.3 = 0.671
  ※ 651 は GPU81% で飽和していないため「651以上」としか言えない。保守的に651を採用。

**モデルの当てはまり**（12条件中10条件が±10%以内）:
1080p/低 が +18.9% ずれる。GPU79%・CPU57%の混在領域で、`min()` では表せない既知の制約。

**シーンのばらつきは約10%**。1080p Perf(593) < 1440p Perf(651) と逆転していることから分かる。
実際のプレイ映像なのでテストシーンを固定できていない。

**「クリエイティブでは33%程度上回る」はユーザー提供の知見**（測定ではない）。
公開マッチでの計測である旨とあわせて、ツールの画面に表示している。

#### Apex の係数の算出過程（2026-09-17）

**出典**: ちもろぐ「Apex Legendsの推奨スペック」https://chimolog.co/bto-apex-legends-specs/
（記事の更新 2024/4/3、CPUデータの更新 2024/11/21）
**許諾**: 取得済み（ユーザーが測定者に確認）。出典の表記は「書いても書かなくてもよい」。
算出方法を公開する方針（絶対ルール3）に沿って、ツールとゲーム別ページに名前とリンクを出している。

**測定構成**

| | GPUの測定 | CPUの測定 |
|---|---|---|
| 構成 | Core i9 13900K / ASUS TUF GAMING Z690-PLUS WIFI D4 / DDR4-3200 16GB×2 / Win11 Pro 23H2 | RTX 4090 + 各CPU |
| 場所 | 射撃訓練場。スモーク・テルミット・スコープ射撃を重ねた重い負荷。測定者いわく「実際のプレイで測ったデータの下位10%に分布する」重さ | キングスキャニオン（実戦マップ）。射撃訓練場ではCPU律速がほぼ出ないため、とのこと。行動を完全には揃えられず誤差多めと明記 |
| 条件 | 1080p 低 / 中 / 最高、1440p 最高、4K 最高 × GPU 32枚 | 1080p / 1440p / 4K 最高 × CPU 20個 |

**設定**: Apex に画質プリセットは無く、低/中/最高は測定者の定義（記事の設定画像より）。

| 項目 | 低 | 中 | 最高 |
|---|---|---|---|
| アンチエイリアス | なし | TSAA | TSAA |
| テクスチャストリーミング割り当て | 最低（2GB） | 中（3GB） | 極（8GB） |
| テクスチャフィルタリング | バイリニア | 異方性2X | 異方性16X |
| アンビエントオクルージョン | 低 | 中 | 高 |
| サンシャドウ範囲 / ディテール | 低 / 低 | 低 / 低 | 高 / 高 |
| スポットシャドウディテール | 低 | 低 | 最高 |
| 空間光 / ダイナミックスポットシャドウ | 有効 / 有効 | 有効 / 有効 | 有効 / 有効 |
| モデル / エフェクトディテール | 低 / 低 | 中 / 中 | 高 / 高 |
| 衝撃マーク / ラグドール | 低 / 低 | 低 / 中 | 高 / 中 |

**読み取り**: グラフは画像で、各バーに数値が印字されている（目視の推定ではない）。
赤の数値が平均、バー内のグレーの数値が 1% Low。後者は記事本文の表（「最低fps（1%）」）と
一致することで確認した。画像のURL（検算はここから再取得してできる）:

```
GPU 1080p 低   https://chimolog.co/wp-content/uploads/2024/04/apex-benchmark-graph-1.jpg
GPU 1080p 中   https://chimolog.co/wp-content/uploads/2024/04/apex-benchmark-graph-2.jpg
GPU 1080p 最高 https://chimolog.co/wp-content/uploads/2024/12/apex-benchmark-graph-3.jpg
GPU 1440p 最高 https://chimolog.co/wp-content/uploads/2024/04/apex-benchmark-graph-4.jpg
GPU 4K 最高    https://chimolog.co/wp-content/uploads/2024/04/apex-benchmark-graph-5.jpg
CPU 1080p 最高 https://chimolog.co/wp-content/uploads/2024/12/apex-benchmark-graph-cpu-1.jpg
CPU 1440p 最高 https://chimolog.co/wp-content/uploads/2024/12/apex-benchmark-graph-cpu-2.jpg
CPU 4K 最高    https://chimolog.co/wp-content/uploads/2024/12/apex-benchmark-graph-cpu-3.jpg
設定の定義     https://chimolog.co/wp-content/uploads/2023/06/apex-benchmark-settings.jpg
```

**原文の数値は下の9条件（selftest に置いたもの）だけ記録する。**
GPU 30枚×5条件＋CPU 19個×3条件を平均と 1% Low の両方で書き写すと約400個になり、
それ自体がちもろぐの表のコピーになるため（絶対ルール1）。Fortnite は1構成15条件だったので全部残せた。

```
GPU測定（+ Core i9 13900K）   平均 / 1% Low
  RTX 4080        4K 最高      127.4 / 66.5
  RTX 4070        1440p 最高   154.1 / 88.3
  RTX 4060        1080p 最高   142.0 / 77.8
  RTX 4070 SUPER  1080p 最高   219.3 / 137.6
  RTX 3060 12GB   1080p 低     151.8 / 82.7
  RX 7600         1080p 中     185.5 / 102.3
  GTX 1650 G6     1080p 最高    50.6 / 32.5
CPU測定（RTX 4090 +）
  Ryzen 5 7500F   1080p 最高   216.9 / 160.1
  Ryzen 7 9800X3D 1080p 最高   298.4 / 192.2
```

**DBとの対応と除外**

- GPU: 32枚中30枚がDBにある。Arc A770 16GB / GT 1030 はDBに無いので除外。
  RTX 4060 Ti は 8GB と 16GB が同じ指数なのでどちらでも同じ、RTX 2060 は 6GB、RTX 3050 は 8GB に対応させた
- CPU: 20個中 Ryzen 5 4500 はDBに無い。Core i5 13400 / 11400 はDBが F 付きモデル
  （13400F / 11400F）なので、同一と断定せず除外した

**GPU側（gpuWeight・gpuScaling・k）**

1. 当初は Fortnite と同じく「4K最高 ÷ Valorant 4K高」の比を1つ取る方針だった。
   ところが比がGPUの性能で系統的に下がる（指数15〜60の13枚で平均0.373・CV 6%、
   指数60以上の11枚で平均0.284、RTX 4090 は 0.210）。
   中位に合わせた比1つだと、指数60〜100の帯が **1080p / 1440p / 4K のどれでも平均+25%（最大+42%）**、
   指数100以上の4Kで平均+42%（最大+59%）高く出た
2. そこで「GPU性能の効き方」を足した（ユーザー承認済み）:
   `GPU由来fps = 464.6 × gpuWeight × (V ÷ 464.6)^gpuScaling × 設定係数 × (画素比)^k`
   （V はそのGPUの Valorant 4K高 推定fps、464.6 は基準GPU RX 9070 XT）。
   gpuScaling が1なら従来の式と一致するので、Valorant と Fortnite の結果は変わらない（ビルド出力で一字一句同じと確認）
3. フィットに使った点: 指数15以上・VRAM 8GB以上の24枚 × 1080p/1440p/4K 最高のうち、
   **平均240fps未満の66点**（CPUと300上限の影響を避けるため）
4. `log(fps)` を最小二乗でフィットし、k と gpuScaling を格子探索（刻み0.001）、gpuWeight はその残差の平均から求めた
5. 結果: **gpuWeight 0.267 / gpuScaling 0.665 / k 0.587**。性能指数が2倍でfpsは約1.6倍

**CPU側（cpuWeight）**: CPU測定の 1080p最高 ÷ Valorant CPU天井 を、DBと名前が一致し平均285fps未満の
15個で平均 = **0.351**（CV 10%、中央値 0.344）。1080p と 1440p はほぼ同じ値（平均差0.6%）で、CPU律速を確認。
9800X3D（298.4）と 7800X3D（291.0）は300上限に近いので除外。

**設定係数**: 1080p で GPU律速がはっきりしている指数15〜45・VRAM 8GB以上の10枚で、
中 ÷ 最高 = **1.21**（CV 2.5%）、低 ÷ 最高 = **1.28**（CV 3.9%）。
低・中は 1080p しか測定が無いので k は最高の 0.587 を流用した（画面にも近似と明記）。

**1% Low の比**: GPU律速の点（上のフィットと同じ条件）で、**上下1割を除いた範囲**を採用した。
点が多いので最小〜最大だと外れ値に引っ張られるため。
最高 0.496〜0.636（66点、中央値0.573）/ 中 0.543〜0.615（16点）/ 低 0.535〜0.619（15点）。
最高の比は解像度で動く（指数15〜60の13枚の中央値で 1080p 0.631 / 1440p 0.569 / 4K 0.518）。

**当てはまり**（ズレ = 予想 ÷ 測定 − 1。GPU測定は CPU 13900K で計算）

| 性能指数 | 4K 最高 | 1440p 最高 | 1080p 最高 | 1080p 中 | 1080p 低 |
|---|---|---|---|---|---|
| 15未満（3枚） | +41% | +24% | +21% | +24% | +24% |
| 15〜45（13枚） | +9% | −2% | 0% | +1% | 0% |
| 45〜60（3枚） | −2% | −5% | +2% | +5% | +7% |
| 60〜100（7枚） | 0% | −2% | +8% | +6% | +5% |
| 100以上（4枚） | −2% | +1% | +2% | −5% | −6% |

指数15以上（VRAM 6GB以下を除く）の120条件で、±15%以内 94% / ±20%以内 98% / 最大のズレ21%。
比1つ（効き方を足さない）場合は 71% / 81% / 59% だった。
CPU測定（RTX 4090 + 各CPU）の1080p最高は平均 −1%（−23〜+12%。CPUの指数側のばらつき）。

**既知の限界（画面の注記に書いている）**

- **指数15未満（GTX 1650 / 1050 Ti / 1060 など）は +15〜50% 高めに出る。** VRAM 4〜6GBのカードが多い。
  ゲーム別ページでは「届く最小のGPU」欄から外し、表の行には「※高めに出る傾向」を付けている
  （`overpredictsBelowIndex`。外す前は 60fps の欄が GTX 1050 Ti＝予想61fps・測定50.8fps になっていた）
- **GPU側は射撃訓練場の重い場面、CPU側はキングスキャニオン。** 場面が違う2つを組み合わせている。
  同じ 13900K + RTX 4090 の 4K最高 でも 169.1（射撃訓練場）と 228.9（キングスキャニオン）で約1.35倍違う。
  軽い場面での倍率は RTX 4090 の1点しか根拠が無いので、画面では倍率を出さず文章だけで伝えている
- CPU律速の条件の 1% Low 比は 0.74〜0.83 で、GPU律速から取ったレンジより高い。CPU律速の構成では
  1% Low が低めに（安全側に）出る
- **1080p最高のグラフ画像だけ 2024/12 に差し替えられている**（低・中は 2024/04）。
  測り直しでゲームのバージョンが違えば、低・中の設定係数が偏る可能性がある。記事からは判別できない
- DirectX 11 / 12 のどちらで測ったかは記載なし。`+fps_max unlimited` の記載も無いが、平均が144を大きく超えているので上限は外れている
- VRAM使用量は記事に「4Kで5GB前後、実プレイで7GB前後」とあるが概数なので、`vram4kMb` は不明（null）にした

**実戦マップでの目安（場面の倍率 1.35・2026-09-17 追加）**

ユーザーの要望「重い場面だけでなく、軽い状況のfpsも出したい」に対応した。

- **出典**: 同じ構成（Core i9 13900K + RTX 4090・4K最高）で場所だけが違う2つの測定。
  GPUの測定（射撃訓練場の重いテスト）169.1 / CPUの測定（キングスキャニオン）228.9 → **1.354、画面では 1.35**
- 4K の RTX 4090 は両方ともGPU律速（キングスキャニオン側は速いCPU 14個が 213.6〜230.5 で横並び）
- **倍率はGPU側だけに掛ける**（`appliesTo: 'gpu'`）。CPU側の係数は元々キングスキャニオンの測定から作っているので、
  CPU側にも掛けると二重に軽くなる。Fortnite の 1.33（ユーザー提供の知見）は全体に掛ける（`'all'`）まま
- **検算**（RTX 4090 + CPU 19個 × 3解像度のキングスキャニオン測定）: 4K の平均ズレが
  重い場面の予想 −16.8% → 倍率あり +2.0%（GPU律速の14個は −6〜+11%）。
  1080p / 1440p はCPU律速なので倍率の有無で変わらず −1%
- **弱点: 根拠のGPUは RTX 4090 の1枚だけ。** 記事の BTO 紹介文（測定ではない）では、
  RTX 5060 の実プレイが最高設定で「160〜180fps前後」（重い場面の予想 165 / 倍率あり 216）、
  RTX 5070 が「240fps」（同 224 / 282）で、実プレイは重い場面と実戦マップの間くらいに見える。
  そのため画面では「上振れ側の目安」と書いている
- 自己テストに2条件（倍率の出どころの4K / CPU律速で伸びない1080p）。倍率を1にすると4K側が名指しで落ちる

**理論値（300fps上限を外した値・2026-09-17 追加）**

ユーザーの要望「どの構成も300と出るので、300が上限だと明記したうえで理論上いくつ出るか見せたい」に対応した。

- `Prediction.uncapped = min(GPU側, CPU側)`。上限で止まるときだけ「理論 ○」を添える
  （fps予想ツール・ゲーム別ページの表・GPU/CPU個別ページの表）
- **300を超える領域は測定で確かめようがない**（ちもろぐ の測定も最大298.4）。画面では「計算上の値で、確かめることはできません」と書く
- ゲーム別ページ（CPU は 9800X3D）の上位GPUは、理論値がほぼ **341 に揃う**。これは 9800X3D のCPU側（970.3 × 0.351）で、
  GPUの差ではなくCPUが上限を決めているため。GPU側の値はfps予想ツールの「GPU側の上限」に出ている
- 「およそ ○〜○」の上側も上限で止めた（以前は 300 のとき「240〜360」と上限を超えて出ていた）
- モニターのHz判定は上限込みの値のまま（実際は300で止まるので、360Hzは「足りない」が正しい）

#### 評価したが不採用にしたソース

| ソース | 判定 | 理由 |
|---|---|---|
| Digital Trends（Fortnite） | 不採用 | 1080p/1440pともCPU律速。指数0.133で解像度がほぼ効いていない。4Kなし |
| ComputerBase（Fortnite） | 不採用 | 作りは最良だが、アップスケーリングが解像度ごとに変わる（1080p Quality / 1440p Balanced / 4K Performance） |
| fpsbench.com | 除外 | 測定値ではなく集約・推定。CPU×GPUの全組み合わせにページがある。TechPowerUp等の集約元の権利も及ばない |

**再利用できる判定基準**:

1. **解像度ごとにアップスケーリングのモードが変わるソースは使えない**。
   解像度の重さとアップスケーリングの効きが混ざり、解像度係数が取れない
2. **CPU×GPUの任意の組み合わせにページがあるサイトは計算機**であって測定サイトではない。
   実測は組み合わせの数だけ手間がかかるので、網羅は物理的に不可能

### 2. 数値をLLMの記憶から出させない

fps値でもスペック値でも、モデルの記憶から「それっぽい数字」を生成させない。
必ず一次ソース（メーカー公式ページ）で確認するか、計算式から導出する。
ゲーマーは数値にシビアで、外れた推定を断定口調で出すと信用を失う。

AIの使いどころは「計算式の設計」と「結果の解説文生成」であって、
「数値そのものの生成」ではない。

### 3. 推定値であることを常に明示する

予想fpsも性能指数も「推定値・誤差±15〜20%」と明記する。
断定的な数値表示はしない。算出方法も公開する。

これは弱みの告白ではなく差別化になる。他サイトは3DMarkスコアを
載せるだけで、それが実ゲームfpsとどう対応するかを説明していない。

### 4. AI推論をユーザー向け機能で多用しない

使われるほど赤字になる。計算・診断・変換はブラウザ内で完結させる。
解説文をLLMで生成する場合は「GPU+CPU+ゲーム+設定」の組み合わせ単位で
キャッシュし、同一条件では再生成しない。

---

## データファイル

| ファイル | 内容 |
|---|---|
| `gpu_specs_draft.csv` | GPU 78件の検証済みスペック（GTX 10〜RTX 50、RX 5000〜RX 9000） |
| `cpu_specs_draft.csv` | CPU 42件の検証済みスペック（Ryzen 5000〜9000、Intel 10〜14世代＋Core Ultra 200S） |
| `gpu_index.csv` | GPU性能指数（算出結果） |
| `cpu_index.csv` | CPU性能指数（算出結果） |
| `make_index.py` | 指数の計算スクリプト。係数を変えて再計算できる |
| `fps_measurement_sheet.csv` | 実測データと導出された係数 |
| `benchmark-list.html` | 一覧ページのプロトタイプ（単一HTML、動作する） |

### スペックデータの検証状況

全120件をメーカー公式資料と照合済み。`verified` 列に状態が入っている。
検証の過程で見つかった誤りは3件（すべてGPUのメモリ周り）:

- RX 5600 XT: メモリ14→12Gbps、帯域336→288GB/s
- RX 5600 XT: ブースト1560→1620MHz
- RX 9070: ブースト2520→2540MHz

CPUは42件すべて誤りゼロだった。

**クロックはすべてリファレンス仕様値で統一している**。
NVIDIAはFounders Editionが別クロックのモデルがある（例: RTX 2070は
リファレンス1620MHz / FE 1710MHz、TDPも175W / 185W）。
混在させないこと。

**ソース間で数値が割れているもの**（Intel公式値を採用）:
- i7-13700K ブースト: Intel公式5.4GHz / 一部メディア5.3GHz
- i5-12600K Pコアベース: Intel公式3.7GHz / 一部メディア3.6GHz

---

## 実測データ（唯一のアンカー）

2026-08-29 測定。構成は以下の1台のみ。

```
GPU     Radeon RX 9070 XT 16GB (ASUS)
CPU     Ryzen 7 9800X3D
RAM     32GB (16GB x2) DDR5
Board   MSI B650 GAMING PLUS WIFI
Monitor フルHD (1920x1080) ※高解像度はAMD VSRで描画
計測    CapFrameX 1.8.6 / 120秒キャプチャ / Remove outliers ON
環境    Resizable BAR: ON / HAGS: ON / Win Game Mode: ON
```

### Valorant 実測4点

| 条件 | 解像度 | 設定 | avg | 1% Low | 0.2% Low | GPU使用率 | 律速 |
|---|---|---|---|---|---|---|---|
| A | 1920x1080 | 全て低 | 970.3 | 676.5 | 605.5 | 57% | CPU |
| B | 3840x2160 | 全て高 | 464.6 | 387.1 | 374.6 | 99% | GPU（完全） |
| C | 2560x1440 | 全て高 | 868.8 | 653.7 | 598.2 | 97% | 混在 |
| D | 3840x2160 | 全て低 | 807.5 | 611.8 | 566.6 | 99% | 混在 |

### 導出された係数

```
CPU天井（Valorant / 9800X3D）  = 970.3 fps
GPU基準点（4K高 / 9070 XT）    = 464.6 fps
解像度係数（4K → 1440p）       = x1.87   ※ピクセル比の理論値は2.25
設定係数（高 → 低 / 4K）        = x1.74
```

解像度係数が理論値2.25より小さいのは、UI描画やCPU処理など
解像度に依存しない部分があるため。実勢としてはこれが正しい傾向。
ただし条件CはGPU Limit Time 85%で純粋なGPU律速ではないため、
この1.87はやや過小評価の可能性がある。

### 1080p の係数（実測なし・外挿）

**2026-08-30 追加。ここは根拠が最も弱い。**

1080pは最も使われる解像度だが、実測の1080pは条件A（全て低）だけで、
そこはCPU律速だったためGPU側の係数が取れていない。
そこで実測の1.87と画素比2.25から指数則の指数を求めて外挿している。

```
1.87 = 2.25^k              →  k = ln(1.87)/ln(2.25) = 0.772
1440p→1080p の画素比        =  (2560x1440)/(1920x1080) = 1.778
1440p→1080p の係数          =  1.778^0.772 = 1.56
```

計算式からの導出であり、記憶から出した数字ではない（絶対ルール2）。
**ただし実測ではないので、ツールの画面上で外挿である旨を明示すること。**
1080pの実測が取れたら、この値を差し替える。

設定係数1.74も4Kでの実測値であり、他の解像度でも同じとみなしている。
これも仮定なので明示する。

---

## 予想fpsの式

```
予想fps = min(GPU由来のfps, CPU由来のfps, ゲーム固有の上限)

GPU由来のfps = 464.6 × gpuWeight × (V ÷ 464.6)^gpuScaling × 設定係数 × (画素比)^k
CPU由来のfps = そのCPUの Valorant 天井 × cpuWeight
```

V はそのGPUの Valorant 4K高 推定fps、464.6 は基準GPU（RX 9070 XT）。
**gpuScaling（GPU性能の効き方）は 2026-09-17 に追加。** 1 なら `V × gpuWeight` と同じで、
Valorant / Fortnite は1。Apex は30枚の測定から 0.665（「Apex の係数の算出過程」）。

低解像度ではCPU項が効き、高解像度ではGPU項が効く。
この構造は実測で検証済み。係数から「1440p低設定」を計算すると
464.6 x 1.87 x 1.74 = 1510fps になるが、CPU天井970.3で頭打ちになる。
これは実測の条件A（1080p低で970.3、GPU使用率57%）と整合する。

### ゲーム固有の上限

| ゲーム | 上限 | 備考 |
|---|---|---|
| Apex Legends | 300 fps | エンジン仕様。`+fps_max unlimited` で既定の144は外せるが、300は超えられない（2026-09 に再確認） |
| Valorant | なし | 設定で上限解除できる |

対応タイトルを増やすときは、毎回この上限を確認すること。
明記しているサイトは少ないので差別化情報になる。

### v1のツールで出さない値（2026-08-30）

根拠が足りないものは**出さない**。画面に「なぜ出さないか」を書く。

- **1% Low** — 実測4点の平均fps比が 0.70 / 0.83 / 0.75 / 0.76 とばらつき、
  固定比では出せない。最低の0.70はCPU律速の条件A、最高の0.83は完全GPU律速の
  条件B。律速側で落ち方が変わる可能性があり、投稿データで検証してから出す
  → **2026-09-09 に公開へ変更。** 上の判断は「1つの固定比では出せない」までは
  正しかったが、**プリセット単位ではまとまる**ことを見落としていた。
  詳細は下の「1% Low の比（2026-09-09）」。判断の経緯を残すためこの項は消さない
- **高負荷時（終盤）の平均fps** — 実測データが存在しない。
  120秒キャプチャに該当する区分が無く、測定方法の定義から必要
- **Valorant以外のタイトル** — 係数がゼロ。Apexは300fps上限のため測定不能
  → Fortnite は 2026-08-30、**Apex は 2026-09-17 に対応。** Apex は基準機では300に張り付くが、
  ちもろぐ の測定には300未満の条件が多数あった（「Apex の係数の算出過程」）

出せるのは「Valorant・低/高プリセット・平均fps」と、
**律速の判定（ボトルネック診断）**まで。診断は min() のどれが効くかで
構造的に決まるため、係数が1タイトル分でも成立する。

---

## 1% Low の比（2026-09-09）

「1% Low を出さない」判断（上の「v1のツールで出さない値」）を覆した記録。

### なぜ覆せたか

旧判断は全条件を1つの比でまとめようとして 0.70〜0.83 のばらつきに阻まれていた。
**画質プリセット単位で分けると、条件によってはきれいにまとまる。**

### 自前の実測（Valorant・CONTEXT.md「Valorant 実測4点」より算出）

| プリセット | 実測点 | 比 | 採用レンジ |
|---|---|---|---|
| 全て高 | 4K(GPU99%) 387.1/464.6 / 1440p(GPU97%) 653.7/868.8 | 0.833 / 0.752 | 0.752〜0.833 |
| 全て低 | 1080p(GPU57%) 676.5/970.3 / 4K(GPU99%) 611.8/807.5 | 0.697 / 0.758 | 0.697〜0.758 |

### Boss Benchmarks の測定（Fortnite・「Fortnite の係数の算出過程」の15条件より算出）

CLAUDE.md 絶対ルール1 に「1% Low ÷ 平均fps」を追加して取得を許可した（2026-09-09）。
比はfps値そのものを復元できないため、データベース著作権・利用規約の観点では
既存の3つの係数と同じ扱い。許諾も取得済み。

| プリセット | 1080p / 1440p / 4K | ばらつき幅 | 採用レンジ |
|---|---|---|---|
| Performance | 0.261 / 0.261 / 0.276 | **0.015** | 0.261〜0.276 |
| 低 | 0.528 / 0.481 / 0.613 | 0.131 | 0.481〜0.613 |
| 中 | 0.427 / 0.542 / 0.601 | 0.173 | 0.427〜0.601 |
| 最高(Epic) | 0.594 / 0.671 / 0.750 | 0.156 | 0.594〜0.750 |

「高」は解像度比較で既に不採用（4Kだけ Lumen ON）なのでここでも使わない。

### ちもろぐ の測定（Apex・2026-09-17）

GPU律速の点が多いので、**上下1割を除いた範囲**を採用した（算出の詳細は「Apex の係数の算出過程」）。

| プリセット | 使った点 | 中央値 | 採用レンジ |
|---|---|---|---|
| 低 | 1080p の15点 | 0.560 | 0.535〜0.619 |
| 中 | 1080p の16点 | 0.584 | 0.543〜0.615 |
| 最高 | 1080p / 1440p / 4K の66点 | 0.573 | 0.496〜0.636 |

CPU律速の点（RTX 4090 + 各CPU）では 0.74〜0.83 と高く、このレンジに入らない。
CPU律速の構成では 1% Low が低めに（安全側に）出る。

### 設計上の決めごと

- **1点ではなくレンジで持つ。** 比が安定している条件は自然に狭いレンジになり、
  「どのくらい信用できるか」が表示にそのまま出る。Performance の 0.261〜0.276 と
  中の 0.427〜0.601 では、画面上での見え方が変わる
- **`games.ts` には小数で書く。** 割り算の式で書くと第三者のfps値そのものが
  コードに残るため。既存の `factor: 525 / 40` は式のままだが、今回は踏襲しない
- **selftest で実測16条件すべての比を検証する。** 宣言レンジが実測比を含まなく
  なったらビルドが落ちる。破壊テストで3条件が名指しで検出されることを確認済み

### 限界（画面にも書いている）

- 自前の実測は1構成のみ、プリセットあたり2点
- Fortnite側は実プレイ映像でテストシーン固定なし（平均fpsで約10%のばらつき）
- ゲーム別・プリセット別であり、GPU/CPUの組み合わせによる違いは反映していない
- Apex はGPU律速の重い場面の比。CPU律速や軽い場面では 1% Low が表示より高く出る

---

## Apex Legends のデータ要件（2026-09-17）

Apex を fps予想に追加する準備。**データが揃うまで有効化しない。**
ベンチマークは他サイト・YouTube から、許諾を取って使う方針。

> **2026-09-17 に対応済み。** ちもろぐ の測定で有効化した（「Apex の係数の算出過程」）。
> 以下は探したときの基準として残す。必須条件との照合:
> 1〜3・5 は満たす。4（数値で読める）はグラフのバーに数値が印字されているので満たす。
> 6（`+fps_max unlimited`）は記載が無いが、平均が144を大きく超えているので外れている。
> 落とし穴のうち「DX11 と DX12 を揃える」は記載が無く確認できていない。
> 「射撃訓練場は実戦より軽い」は、ちもろぐ の測定では逆（あえて重い負荷をかけている）だった。

### 未解決: 自分の観察と予想が食い違う

**自分のPC（RX 9070 XT + 9800X3D）の記録は「1440p最高でも4K最低でも300に張り付く」だが、
ツールの予想は 1440p最高で約200fps、4K低で約160fps（どちらもGPU律速）になる。** 約1.5〜2倍の差。

考えられる理由（どれかは判別できていない）:

- **場面の重さ。** ちもろぐ のGPU測定は、実プレイの下位1割に入る重い場面。
  同じ 13900K + RTX 4090 の 4K最高 でも、実戦マップでは射撃訓練場の約1.35倍出ている
- **「最高」「最低」の中身が違う。** Apex にプリセットは無く、ちもろぐ の最高はテクスチャ「極」など
- **自分の記録は CapFrameX の測定ではない**（数値が残っていない。「うまく取れなかった」）。
  張り付いた瞬間を見た可能性がある

**解消するには、自分のPCで CapFrameX を使い、射撃訓練場で ちもろぐ と同じ設定・負荷
（スモーク・テルミット・スコープ射撃）の 1440p最高 と 4K最高 を測る。**
予想（約200 / 約124fps）と±20%で合えば係数の裏付けになる。大きく外れたら係数を見直す。

「サイトに載せられる独自の発見」の「この構成はApexにはオーバースペック」は、
この食い違いが解消するまでサイトに出さないこと。

### 300fps上限が前提を変える

2026年時点でも**300fpsはエンジンのハード上限**（既定の144は `+fps_max unlimited` で外せる）。
上限に張り付いた測定からは係数が取れないので、**300未満のデータが必要**になる。

基準機（RX 9070 XT + 9800X3D）は1440p最高でも4K最低でも300に張り付くため、
**解像度係数・設定係数を取るには性能が半分以下の中位GPUの測定が要る。**

### 自分のPCの記録に穴がある

「1440p最高でも**4K最低**でも300に張り付く」と記録しているが、**4K最高が無い。**
これが300未満なら、基準GPU（指数100）そのもので GPU側の重さ（`gpuWeight`）が取れる。
**許諾が要らず、最も強い根拠になる。最優先で測ること。**

第三者データからも同じ値を換算し、**自前の値と一致するか照合する**。
一致すればモデルの妥当性の裏付けになる。

### 必須条件（1つでも欠けたら使わない）

1. 同じPC構成で、解像度か画質だけを変えた測定が複数ある
2. **GPUが当サイトのDB（78モデル）に載っている**（基準GPUとの換算に使う）
3. **平均fpsが明確に300未満**（299〜301は上限に張り付いていて使えない）
4. **平均fpsと1% Low が数値で読める**（グラフの目視読み取りは使わない）
5. 構成（GPU / CPU / メモリ）と設定（プリセット / 解像度 / アップスケーリング）が明記
6. **`+fps_max unlimited` を入れている**

### Apex 特有の落とし穴

- **144前後で平らに揃っている** → 既定の144上限を外し忘れている
- **300前後で揃っている** → エンジン上限。係数に使えない
- **射撃訓練場は実戦より軽い** → どこで測ったか明記が必要
  （Fortnite の公開マッチ／クリエイティブと同じ構図）
- **DX11 と DX12 を揃える**
- 解像度ごとにアップスケーリングが変わるものは使えない（Fortnite と同じ）
- **1% Low の比は、平均が300未満の条件からだけ取る。**
  平均が上限で削られていると比が歪む。Fortnite には無かった問題

### 探す性能帯

基準機の半分以下（DBの指数15〜55）。例: RTX 4060 / RTX 5060 / RTX 3060 /
RX 7600 / RX 9060 XT / RX 6600。GPU使用率が画面に出ているものが最良
（CPU律速かどうかの判定に使う）。**この性能帯でApexが300を下回るかどうかは
当サイトでは確認していない**ので、実際の数値を見て判断すること。

### 既知のリスク

**CPU側の重さ（`cpuWeight`）が取れない可能性がある。** 9800X3D では CPUが縛る前に
300上限に当たる。弱いCPUと強いGPUを低設定で組んだ測定が見つからない場合、
CPU側を「未測定」として扱う方針を別途決める必要がある。

### 有効化するときの手順

コード側の準備は済んでいる:

- 300fps上限の扱いは実装済み（`model.ts` の `bottleneck: 'cap'`、`build.ts` の `reason: 'cap'`）
- `supported` を true にするだけで、ゲーム一覧・個別ページ・サイトマップ・逆引きツール・
  fps予想、**そしてページに書かれたゲーム名（6か所）も自動で追随する**
  （`supportedGameNames()`。ダミーのプリセットで有効化して確認済み）
- **presets が空のまま有効化すると、分かる言葉でビルドが止まる**（`games.ts` の検証）。
  以前は「Reduce of empty array」という原因の分からないエラーで落ちていた

残りは Fortnite と同じ: `games.ts` に係数を入れる → `selftest.ts` に実測再現ケース →
`build.ts` の `INVERSE_CASES` → このファイルに出典・原文の数値・算出過程・許諾。
（2026-09-17 にこの手順で有効化した）

---

## 性能指数の算出方法

`make_index.py` に実装済み。入力は公開スペックとメーカー公表IPCのみ。

### GPU

```
compute    = シェーダー数 x アーキ効率係数 x ブーストクロック(GHz)
cache_mult = 1 + min(InfinityCache, 128) / 128 * 0.60
mem        = 帯域幅 x cache_mult
index      = compute^0.78 x mem^0.22        → RX 9070 XT = 100 に正規化
```

アーキ効率係数（重要）:

```
Pascal 1.00 / Turing 1.15 / Ampere 0.66 / Ada 0.78 / Blackwell 0.82
RDNA 0.95 / RDNA2 1.05 / RDNA3 1.08 / RDNA4 1.50
```

**NVIDIAのAmpere以降はCUDAコアがFP32倍カウント表記**のため係数を下げている。
AMDはRDNA全世代で「CU x 64」の一貫表記なので倍カウント補正は不要。
※ここを取り違えるとRX 6950 XTが7900 XTXより上位に来る（実際に一度やらかした）。

RDNA4が1.50と突出しているのは、64CUの9070 XTが前世代96CUの7900 XTXと
拮抗する事実を反映したもの。

### CPU

```
st        = メーカー公表IPC x ブーストクロック
cache_f   = 1 + 0.22 * ln(L3 / 32)
core_f    = 0.80 + 0.20 * min(コア数, 8) / 8      ※8コアで飽和
index     = st x cache_f x core_f x penalty       → 9800X3D = 100 に正規化
```

IPC（Zen3 = 1.00 基準、メーカー公表値ベース）:

```
Zen 3 1.00 / Zen 4 1.13 / Zen 5 1.31
Comet Lake 0.82 / Rocket Lake 0.95 / Alder Lake 1.10
Raptor Lake 1.12 / Raptor Lake-R 1.12 / Arrow Lake 1.19
```

**デュアルCCDの扱い（重要）**:

- 非X3Dで9コア以上のRyzen: L3は32MBx2に分割される。ゲームは片方のCCDで
  動くため実効L3は半分。※これを見落とすと9950Xが9800X3Dより上位に来る
- X3Dで9コア以上: 実効L3は96MB（キャッシュ側CCDの容量）。
  実効クロックもキャッシュ側CCDの低い方になるため boost - 0.5GHz で近似
- 12コアX3D（9900X3D / 7900X3D）: キャッシュ側CCDが6コアしかないため
  penalty 0.93 を追加

### この指数の限界（必ず表示すること）

- 実測は9070 XT + 9800X3Dの**1構成のみ**。他モデルは理論値からの外挿
- 誤差は±15〜20%程度
- **近接したモデル同士の順位は信用できない**。RTX 4060とRX 7600のような
  数%差の組み合わせは逆転しうる。アーキごとに係数を1つしか持てず、
  同じアーキでも上位ダイと下位ダイでキャッシュ量が違うため
- 世代をまたいだ大きな差はそれなりに信用できる
- Intel Arcは合成ベンチのスコアほど実ゲーム性能が出ない傾向があり、
  この方式では特に外れる可能性が高い（現在データ未収録）

---

## VRAMについて

**平均fpsにはほとんど影響しないが、1% Lowには強く効く。**
VRAM不足はカクつきの主因なので、独立した判定軸として持つこと。

実測で得られた基準（Valorant 4K）:

| 条件 | VRAM使用量 |
|---|---|
| 4K 全て高 | 8.19 GB |
| 4K 全て低 | 8.41 GB |

**画質を下げてもVRAMは減らなかった**（むしろ増えた）。
「設定を下げればVRAM不足が解消する」という案内は誤りになりうる。

4K高設定で8.19GB使うため、**8GBカード（RTX 4060、RX 7600等）では不足する**。
16GBの検証機では8GBカードのカクつきを再現できないため、
この領域はユーザー投稿で埋める必要がある。

---

## ユーザー投稿機能の設計要件

実測データを集める仕組みが、このサイト唯一の模倣不可能な資産になる。
公開ベンチが手薄な「設定別fps」「1% Low」「終盤高負荷時」こそ価値がある。

投稿フォームに必須で入れる項目:

- **CapFrameXの Remove outliers をONにした値を送ってもらう**
  ※これを指定しないと投稿者ごとに0.1% Lowが10倍以上ばらつく
- 計測前のウォームアップ（5分程度のプレイ）を指示する
  ※冷えた状態だとブーストが高めに出る。また起動直後はシェーダー
  コンパイルで巨大なスパイクが出て0.1% Lowが壊れる
- GPU使用率ではなく **GPU Limit Time** で律速判定する
  ※使用率97%でもGPU Limit Time 85%＝純粋なGPU律速ではないケースがある。
  使用率だけ見ていると見抜けない
- 解像度・画質設定・アップスケーリングの有無・ドライバ版・
  Resizable BAR / HAGS の状態

---

## 実装状況と次の作業

### できているもの

- スペックDB（GPU 78 / CPU 42、全件検証済み）
- 性能指数の算出ロジック
- 一覧ページのプロトタイプ（`benchmark-list.html`、単一HTMLで動作）
  - GPU/CPUタブ、検索、メーカー・アーキ絞り込み、全列ソート
  - 行タップで詳細展開、最大3モデルの比較
  - ソート中の指標が行の背景バーとして可視化される
  - モバイルではカード表示

### 次にやること（優先順）

1. **一覧ページに指数の列を追加**
2. **個別モデルページ**（120モデル分）。SEOの本命はここ。
   「RTX 5070 スペック」等の検索を拾う入口が120本できる。
   ただしスペック表だけだと薄いコンテンツ判定のリスクがあるので、
   同世代・同価格帯との違いなど固有情報を足す必要がある
3. **ユーザー投稿機能**
4. **fps予想ツール本体**

> **2026-08-30 変更（ユーザー承認済み）**: 3と4を入れ替えた。
> 実測データが集まるにはカレンダー時間がかかるため、収集の口を先に開けないと
> 予想ツールに着手する時点でデータが1台分のまま増えていない。
> 下の「公開の順番についての判断」（投稿を集めてから予想ツールを完成させる）と
> 元の並びが食い違っていたのを、公開の順番側に揃えた形。
> 実装単位への分解は `ROADMAP.md` を参照。

### 公開の順番についての判断

> **2026-08-30 変更（ユーザー承認済み）。以前の判断は下に残す。**
>
> **fps予想ツールを一覧ページの次に作る。** 個別モデルページと投稿機能より先。
>
> 理由: スペックDBだけでは他サイトと差別化できず、訪問理由にならない。
> ツールがサイトの訪問理由になる。サイトは複数のツールとゲーマー向け記事を持つ
> 収益サイトとして設計する方針になった。
>
> **精度の懸念（下の旧判断）は、対応範囲を絞ることで担保する。**
> ボトルネック診断を主役にし、fpsの絶対値は実測の裏付けがある範囲
> （Valorant・低/高プリセット・平均fpsのみ）に限定する。
> 出さない値と理由は「v1のツールで出さない値」に記載。
> 律速の判定は min() の構造から決まるので、係数が1タイトル分でも成立する。

**旧判断（2026-08-29）:**

一覧ページを先に出して集客し、来訪者から実測投稿を集めて
予想ツールを後から完成させる。

予想ツールを先に出すと、精度が低い状態で公開することになり、
数値にシビアなゲーマー相手だと信用を失う。

この懸念自体は今も有効。**だから範囲を絞る**という形で対応している。
範囲を広げるとき（対応ゲームを増やす等）は、必ず実測を先に取ること。

---

## サイトに載せられる独自の発見

実測の過程で得られた、他サイトにない情報。

- Apexは300fps上限（エンジン仕様、解除不可）
- **RX 9070 XT + Ryzen 7 9800X3D は、Apexでは1440p最高設定でも
  4K最低設定でも300上限に張り付く**＝この構成はApexにはオーバースペック。
  「設定を下げても無駄」を教えてくれるツールは少ない
  → **2026-09-17: ちもろぐ の測定から作った予想（1440p最高で約200fps）と食い違うため保留。**
  CapFrameX で測り直すまでサイトに出さない（「Apex Legends のデータ要件」の「未解決」）
- Valorantの解像度スケーリングは実測1.87倍（理論値2.25倍より緩やか）
- Valorant 4K高設定でVRAM 8.19GB＝8GBカードでは不足
- 画質を下げてもVRAMは減らない
- GPU使用率97%でもGPU Limit Time 85%＝純粋なGPU律速ではない
- **Fortnite の Performance（競技用）設定は、1% Low が平均の 26〜28% しか出ない。**
  平均651fpsでも実際は170fps付近まで落ちる。3解像度で 0.261/0.261/0.276 と
  ほぼ一致しており、偶然ではない。競技勢がまさに選ぶ設定なのに、
  この落差を書いているサイトが無い。Valorant は同じ指標で 0.70〜0.83

---

## アフィリエイトの判断（2026-09-12）

Amazonアソシエイトのリンクを入れた。価格は入れていない。

### 価格を出せない理由（調査で判明）

| 事実 | 出典 |
|---|---|
| PA-API 5.0 は**廃止済み。現在403を返す** | `webservices.amazon.com` が `paapiv5-deprecation` にリダイレクト |
| 後継は **Creators API** | 同上 |
| Creators API は**「直近30日で10件以上の適格販売」が必要** | Amazon公式ドキュメント "Have at least 10 qualifying sales within the past 30 days" |
| 商品リンクを貼るだけなら**API不要・資格要件なし** | 資格要件はAPIに対するもの |

**順番が必然的に決まる:**

```
リンクを貼る → 売上が10件/30日 → Creators API → 価格取得 → 本当のコスパ構成
```

逆引きツールの「コスパ構成」が価格を見ていないのはこのため。
Creators API の要件を満たしたら、価格で並べ替える形に変えられる。

### 信用を守るための決めごと

ROADMAP フェーズ5 の「無関係な商品を出すと信用を落とし、推定値であることを
明示する方針と相性が悪い」への対策。

- **`src/lib/fps/build.ts` は `affiliate.ts` を import しない。**
  推奨の順番は性能指数だけで決まり、広告は一切影響しない。
  **この依存関係を作らないこと。信用の担保そのもの**
- 逆引きツールの画面に「候補は性能指数だけで選んでおり、広告の有無は
  順番に一切影響していません」と明記している
- タグ未設定のときは**リンクも開示文も出さない**。
  リンクが無いのに「収入を得ています」と書くと事実に反する

### 実装上の決めごと

- **商品ASINでは紐付けない。** 120モデル分を人手で対応付ける必要があり、
  型番違い・在庫切れ・新モデルで必ず陳腐化して維持できない。
  **モデル名からの検索URL**なら自動生成でき、在庫切れにも強い
- **`rel="sponsored"` は必須**（Google「外部リンクの修飾」）。
  付けないと有料リンクの未申告になる。
  `noreferrer` は付けない（成果計測を妨げる可能性）
- フッターの開示文は**運営規約で指定された文言**。勝手に変えない

### 法的な位置づけ

ステマ規制（景品表示法・2023-10-01施行）の**規制対象は商品を供給する事業者
（広告主）であり、アフィリエイター自身ではない**（消費者庁）。
ただし**アソシエイト運営規約が開示文の表示を義務づけている**ので、
契約上の理由で表示が必要。根拠が違うだけで、やることは同じ。

### 検索結果の精度（2026-09-12 追記）

実際にリンクを踏んで判明した問題。「GeForce RTX 3050 8GB」で検索すると
上位5件が **RTX 5060（広告）/ RX 9060 XT（広告）/ 3050の6GB版×3** で、
**8GB版が1件も出なかった**。しかも広告の2件は推奨品（約¥38,000）に対して
¥65,455と¥73,981。「安く済ませたい人に高いものを買わせる導線」になっていた。

対策:

- 検索語にカテゴリ名を足す（GPU → 「グラフィックボード」、CPU → 「CPU」）
- `i=computers` でパソコン・周辺機器に限定
- **スポンサー広告そのものは消せない**（Amazonの仕様）ので、
  リンクの下に「型番とVRAM容量を確認してから購入してください」を出す

Amazonは自動取得を拒否する（503）ため、**改善幅は当サイト側からは検証できない。**
実際にクリックして目視で確認すること。

**2026-09-12 追記: 上の対策のうち2つは逆効果だったので撤回した。** 実機で確認した結果:

```
「RTX 5060 グラフィックボード」＋価格の安い順
  → upHere グラボステー ¥1,480（アクセサリ。商品名に「汎用グラフィックボード」が
     入るのでカテゴリ語で拾ってしまう）
  → RTX 2060 SUPER 整備済み品 ¥19,800（別モデル・中古）
  → RTX 5060 Ti（別グレード）

「GeForce RTX 5060」だけ・おすすめ順
  → MSI GeForce RTX 5060 8G ¥65,455（正解）
```

**最終形は「モデル名だけ＋ i=computers ＋ 並べ替え指定なし」。**
モデル名は十分に特徴的なので、余計な語を足さない方がよい。

並べ替えをやめたので、**画面に「最安」と書かないこと。**
並べていないのに最安と書くと果たせない約束になる。
現行世代は「価格を見る」、旧世代は「中古も含めて探す」にしている。

### 推奨が古いモデルに寄る問題（2026-09-12）

逆引きツールは「目標を満たす最小」を出すため、**必然的に古いカードを指す**。
データで確認したところ、**性能指数が下位30件のうち28件が2022年以前**だった。
GTX 1070（2016年）を新品で買うことはできないので、買い物ガイドとして成立しない。

`src/lib/generation.ts` を追加し、アーキテクチャで「現行＋1つ前の世代」を判定する。
**発売年で切らない**のは、RTX 4090（2022年）のような現役モデルが落ちるため。

- 逆引きツールに「新品で買える世代に絞る」を追加。**既定でON**
- 絞ると GPU 29/78件・CPU 27/42件。最低は RTX 5050 と Core i5-13400F
- 絞り込みで候補が消えた場合は `generation` という専用の理由を返し、
  「絞り込みを外すと出ます」と案内する
- 古いモデルには発売年と「新品での入手は難しい可能性があります」を添える。
  **在庫は確認していないので断定しない**

**世代が変わったら `generation.ts` のアーキテクチャ一覧を更新すること。**

### 設置場所

個別モデルページ120本（GPU 78 + CPU 42）と、逆引きツールの候補。
**一覧表には入れていない。** ROADMAP の「一覧は検索流入の受け皿なので
体験を優先する」に従った。

---

## 構成の逆引き（2026-09-12）

「ゲーム・設定・目標fps」から必要なGPUとCPUを出すツール（`/tools/build`）の設計記録。

### 逆引きは探索が要らない

予想の式は `fps = min(GPU由来, CPU由来, ゲーム上限)`。
`predict()` が GPU由来と CPU由来を**別々に**返すので、
「GPU由来 ≧ 必要fps」「CPU由来 ≧ 必要fps」を満たす最小のモデルを選ぶだけでよい。
片側だけを見たいときは、もう片方に `Infinity` を渡して `min()` を無効化している。

### 3段階の定義（価格データが無いため、価格以外の軸で分けた）

`data/*.csv` に価格の列が無く、**円あたりの性能は出せない**。
ユーザー要望の「コスパ／余裕／安定」を、次のように定義し直した。

| 段階 | 定義 | 根拠 |
|---|---|---|
| コスパ | 平均fpsが目標をちょうど満たす最小構成 | 目標に対して無駄がない |
| 余裕 | 平均fpsが目標 × 1.2 | 誤差 ±20% の下振れをちょうど吸収する |
| 安定 | **1% Low が目標を満たす** | 落ち込んでも目標を割らない |

**「コスパ」は価格を見ていない。** 画面上でその旨を明記している。
誤解されたまま使われると嘘になるため、この注記は消さないこと。

安定構成は `lowRatio` を使う。他サイトが持っていない軸。

### 絞り込みの条件

- **VRAMは必ず条件に入れる。** fpsが足りていてもVRAMが不足すると
  カクつきの主因になる（`vramNeedMb()` を `diagnose.ts` から export して共用）
- メーカー指定（GeForce / Radeon / Intel / Ryzen）は `vendor` で絞る。
  「条件は満たすがメーカー指定で消えた」場合は、別の理由として画面に出す
- 候補は最小1件ではなく**指数の低い順に4件**返す。
  「近接モデルの順位は信用できない」ため、1つに断定しない

### 出せない場合を型で区別する

`cap`（ゲーム上限超え）/ `noRatio`（1% Lowの実測が無い）/
`none`（掲載モデルでは届かない）/ `vendor`（メーカー指定で該当なし）。
理由ごとに言うべきことが違うので、画面側で出し分ける。

### 検証は自己テストで固定した

`assertBuildInverts()` を `/tools/build` の Server Component から呼んでいる。
逆引きの結果を `predict()` に入れ直し、本当に目標を満たすか・VRAMが足りているか・
**1つ下のモデルでは届かないか（本当に最小か）**を7条件で確認する。
逆引きと予想が食い違ったらビルドが落ちる。
候補の並び順を反転させる破壊テストで、検出されることを確認済み。

### 実データでの結果

```
Fortnite / 1080p / Performance / 240fps
  コスパ RTX 3060 8GB + Core i9-10900K
  余裕  RX 5700 XT   + Core i9-10900K
  安定  必要平均920fps → CPUが存在しない
```

安定が「不可能」になるのは欠陥ではなく情報。
CPU天井が651fpsなので、この設定でカクつきの底を240fpsに保つのは
**どんなCPUでも不可能**。競技設定は平均fpsは出るがカクつく、を数字で示せる。

---

## 見出しフォントの判断（2026-09-09）

### 何が問題だったか

見出し用クラス `font-cond` の実体は **IBM Plex Sans Condensed** で、
この書体には**日本語のグリフが1文字も入っていない**。そのため:

- h1「PCアンカー」は「PC」= Plex Condensed / 「アンカー」= 端末の既定フォント
  という混在状態で、1つの単語の中で字幅も太さも違ったまま拡大されていた
- 日本語だけの見出し（サイト全体で71か所）は Plex に一切当たらず、
  素の system-ui で出ていた

つまり**日本語用の見出し書体が存在しなかった**。

### next/font で日本語が使えるかの検証

Next の font-data.json はどの書体も `subsets` に `japanese` を持たないが、
`subsets` は **preload するファイルを選ぶ指定**でしかなく、CSS に載っている
全 unicode-range のファイルはダウンロードされる
（`node_modules/next/dist/compiled/@next/font/dist/google/loader.js:102`）。

実測（Zen Kaku Gothic New・2ウェイト）:

```
@font-face   243（約121チャンク × 2）
日本語域      U+4E00（CJK統合漢字）U+3041（ひらがな）を確認
CSS          179KB → brotli 22KB（Cloudflare が自動で brotli 配信）
本体          平均 11KB/チャンク。unicode-range で必要な分だけ遅延取得
```

### 決めたこと

| 用途 | 書体 | 理由 |
|---|---|---|
| 日本語の見出し（71か所） | **Zen Kaku Gothic New 700/900** | 読みやすさ優先。飾りは入れない |
| ロゴ（ヘッダー＋ヒーロー） | **Rubik Distressed 400** | 輪郭がざらつくが字の構造は壊れない |
| 英数字・数値 | IBM Plex Sans Condensed（従来通り） | 変更なし |
| 本文 | システムフォント（従来通り） | 日本語ウェブフォントは重い |

`--font-cond` は **Plex Condensed を先、日本語書体を後**の順にしてある。
フォント選択は1文字ずつ行われるので、英数字（GPU名・fps値・指数）は
Plex Condensed の締まった字面のまま、Plex に無い日本語だけが落ちる。
**この1行で71か所すべてが直る**ので、見出しのマークアップは触っていない。

### ロゴの書体を選んだ経緯

最初に日本語のディスプレイ書体を3案（Zen Kaku / M PLUS 1 / IBM Plex Sans JP）
出したが「どれも普通」との評価。**英語名を主役にする**構成に変えたことで
欧文ディスプレイ書体が使えるようになり、選択肢が大きく広がった
（欧文は1書体1ファイル・数十KB。日本語書体の1/50以下）。

崩れ系6案 → 9案と絞り込み、**崩れが最も控えめな Rubik Distressed** を採用。
構造が壊れないため 20px のロゴサイズでも読めることが決め手。

比較用に使った `?font=` の切り替え（配色の `?theme=` と同じ仕組み）と
`FontPicker.tsx` は、決定後に削除済み。**本番に出ているのは1書体だけ。**

### OGP画像（2026-09-09）

SNSでリンクが貼られたときのカード。**Google の検索結果には出ない**ので
検索流入には効かないが、サイトの見た目と食い違っていると印象が悪い。

- 背景（線画の錨）は生成AIで作った。`assets/og-source.jpg`
- **文字は生成AIに描かせていない。** 日本語は確実に崩れるため、
  背景だけ生成させ、サイトと同じフォントで `scripts/make-og.mjs` から重ねている
- 生成は `npm run og`。**ビルドには組み込んでいない**（フォントをネットから
  取るため、ビルドが外部の可用性に依存してしまう）。出力をコミットして配信する

作り直しが要るのは、掲載モデル数が変わったとき・サイト名やキャッチコピーを
変えたとき・背景画像を差し替えたとき。**画像に焼き込む件数は data/*.csv から
数えている**ので、再生成すれば自動で追随する（手打ちしない）。

技術的な制約と実測:

| 項目 | 内容 |
|---|---|
| Satori のフォント | WOFF2 非対応。TTF が要る |
| 日本語フォント | Google Fonts の CSS API は欧文サブセットしか返さないため、リポジトリから直接取る（Zen Kaku Black 2.2MB） |
| 元画像の比率 | 1584x672 (2.36:1)。OGPは 1.91:1 なので縦が足りない |
| 切り取り | 錨の発光が右端4pxまで届いているため右は削れない。左を240px削り、残りは上下の帯で合わせた |
| 帯の色 | `#020a0c`。元画像の上下端が全幅で均一な黒（実測 #01090b〜#030d0f）だったので継ぎ目が出ない |
| 形式 | PNGだと1.1MB。JPEG品質90（色間引きなし）で163KB、劣化なし |

### ヒーローの構成

```
GAMING PC FPS PREDICTOR   mono・アクセント色
PC ANCHOR                 Rubik Distressed・最大 7rem（主役）
── PCアンカー              Zen Kaku・添え物
```

h1 には**英語名と日本語名の両方**を入れてある。どちらで検索されても拾え、
読み上げも「PC ANCHOR / PCアンカー」と自然につながる。
ヘッダー左上のロゴも同じ書体・同じ英語表記に揃えた。

---

## 技術スタック

- Next.js / TypeScript / Tailwind CSS / Supabase / Vercel
- Claude Code（VS Code拡張）で実装、手動承認モードを使用
- 複雑なタスクではプランモードを使う
