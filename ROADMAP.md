# ROADMAP — 作るものと、その順番

## このファイルの位置づけ

| ファイル | 役割 |
|---|---|
| `CLAUDE.md` | 守るべきルール。毎回読む |
| `CONTEXT.md` | なぜそう判断したかの記録 |
| `ROADMAP.md` | **何をどの順で作るか**（このファイル） |

優先順位は `CONTEXT.md`「公開の順番についての判断」に従っている。
ここと矛盾する変更を入れたくなった場合は、先に `CONTEXT.md` 側を更新すること。

実装単位はチェックボックスで管理する。着手・完了のたびにこのファイルを更新する。

---

## サイトの形

**複数のツールとゲーマー向け記事を持つ収益サイト**として設計する。
スペックDBはその一部であって、全体ではない。

```
/                  トップ。ツール一覧と新着記事
/tools             ツール一覧
/tools/fps         fps予想・ボトルネック診断
/gpu  /cpu         スペック一覧（URLは変えない。検索語に近く短いため）
/gpu/[slug]        個別モデルページ
/articles          記事一覧
/articles/[slug]   記事
```

ツールは `/tools/` 配下に増やしていく。記事は MDX ファイルで持つ想定
（CMS費用ゼロ・Gitで履歴が残る）。

## 全体像

```
フェーズ0  土台（完了）
    │
フェーズ1  一覧ページ（完了）
    │
フェーズ2  fps予想・ボトルネック診断 ─┬─→ フェーズ3  投稿機能
                                      │
                                      └─→ フェーズ4  個別モデルページ ─→ フェーズ5  記事・収益化
```

| # | フェーズ | 位置づけ | 状態 |
|---|---|---|---|
| 0 | 土台（Next.js セットアップ） | — | 完了 |
| 1 | スペックDB 一覧ページ | 集客の入口 | 完了 |
| 2 | **fps予想・ボトルネック診断** | **中核機能・訪問理由** | 着手中 |
| 3 | ユーザー投稿機能 | 唯一の模倣不可能な資産 | 未着手 |
| 4 | 個別モデルページ 120本 | SEOの本命 | 未着手 |
| 5 | 記事と収益化 | — | 未着手 |

### フェーズ順を変えた理由（2026-08-30・ユーザー承認済み）

元は 2=個別ページ / 3=投稿 / 4=ツール の順だった。
スペックDBだけでは他サイトと差別化できず訪問理由にならないため、
**中核機能を前倒しした**。

`CONTEXT.md`「公開の順番についての判断」にあった精度の懸念は今も有効で、
**対応範囲を絞ることで対応している**（フェーズ2の項を参照）。

## 常設機能 — テーマ選択

配色を訪問者が切り替えられる。ゲーミング周辺機器のRGB文化と相性がよく、
サイトの手触りとして残す。

- テーマ＝アクセント色＋背景の色味のひと組。名前付きで選ぶ
- 初期は CYAN / AMBER / LIME。追加はCSSブロック1つで済む形にする
- 選択は端末に保存。`?theme=` でも指定でき共有できる
- 追加するテーマは必ずコントラストを検証してから入れる

## フェーズ0 — 土台（完了）

- [x] Next.js 16 / TypeScript / Tailwind CSS 4 / App Router / `src/` 構成
- [x] git 初期化・初回コミット

---

## フェーズ1 — スペックDB 一覧ページ（完了）

GPU 78件・CPU 42件のスペックと性能指数を一覧で見せるページ。
`CONTEXT.md`「次にやること」#1 にあたる。

### なぜ必要か

- **集客の入口**。「RTX 5070 スペック」等の検索流入を受ける最初の受け皿
- **投稿機能の土台**。人が来ないと実測投稿は集まらない。
  投稿を集めるにはまず一覧ページで流入を作る必要がある
- プロトタイプ（`prototype/benchmark-list.html`）が既に動いており、
  最も早く公開できる状態にある

### 実装単位

- [x] **1-1 データ層**。`data/*.csv` を唯一の正とし、ビルド時に型付きデータへ変換する。
      CSVを更新して再ビルドすれば反映される形にする
      （プロトタイプは HTML 内に `const DATA = {...}` を直接埋め込んでいる。
      この方式だとCSVとHTMLの二重管理になるため引き継がない）
- [x] **1-2 GPU一覧ページ** 78件
- [x] **1-3 CPU一覧ページ** 42件
- [x] **1-4 性能指数の列を追加**。`gpu_index.csv` / `cpu_index.csv` を結合する。
      プロトタイプには指数列が無いため、ここが実質的な新規機能
- [ ] **1-5 指数の説明ページ**。算出式・アーキ効率係数・限界を公開する
      （`CLAUDE.md` 絶対ルール3「算出方法も公開する」）

プロトタイプから引き継ぐ機能: GPU/CPUタブ、検索、メーカー・アーキ絞り込み、
全列ソート、行タップで詳細展開、最大3モデルの比較、
ソート中の指標を行の背景バーで可視化、モバイルのカード表示。

### 完成の判定基準

- GPU 78件・CPU 42件が全て表示され、件数がCSVと一致する
- 性能指数の列でソートでき、`gpu_index.csv` の順位と一致する
- すべてのページに「推定値・誤差±15〜20%」と算出方法へのリンクがある
- `data/*.csv` を書き換えて再ビルドすると表示に反映される
- プロトタイプの機能が欠けていない（特にモバイル表示）

### 依存関係

フェーズ0のみ。**他のどのフェーズにも依存しない**ので、ここから着手できる。

### 想定される難所

- **指数の列を出すと順位が独り歩きする**。`CONTEXT.md`「この指数の限界」の通り、
  アーキごとに係数を1つしか持てないため近接モデルの順位は信用できない。
  RTX 4060 と RX 7600 のような数%差は逆転しうる。
  順位を断定しない見せ方（近接帯をひとまとめに表示する等）の設計が要る
- プロトタイプは単一HTMLで完結しており、Reactの状態管理へ移す際に
  ソート・絞り込み・比較の組み合わせで挙動が変わりやすい
- 120件を一度に描画するため、モバイルでの表示性能

---

## フェーズ2 — fps予想・ボトルネック診断（中核機能）

GPU・CPU（任意でメモリ・電源）とゲーム・解像度・設定を入れると、
推定fpsと**どこがボトルネックで何を変えれば伸びるか**を返す。`/tools/fps`。

### なぜ必要か

- **サイトの訪問理由になる**。スペックDBだけでは他サイトと差別化できない
- 診断結果がそのままアフィリエイトの出口になる（フェーズ5の前提）
- 「どこを変えれば伸びるか」を教えるツールは少ない。
  実測で得た「Apexは設定を下げても300上限に張り付く＝オーバースペック」の
  ような知見は、そのままこのツールの価値になる

### 対応範囲（v1）— 実測の裏付けがある範囲に絞る

`CONTEXT.md`「公開の順番についての判断」の精度の懸念は、ここで担保する。

- **ゲーム: Valorant のみ**。他タイトルは係数がゼロなので出さない。
  Apexは上限300fpsの説明のみ（エンジン仕様で測定できない理由も書く）
- **解像度: 1080p / 1440p / 4K**。1080pは実測がなく外挿なので明示する
- **設定: 低 / 高 の2プリセットのみ**。実測がこの2点しかない
- **出力は平均fpsのみ**。1% Lowと高負荷時は根拠が足りないので出さず、
  「なぜ出さないか」を画面に書く（これ自体が差別化になる）

### 実装単位

- [ ] **2-1 予想エンジン**。`min(GPU由来, CPU由来, ゲーム上限)`。純粋関数。
      **ブラウザ内で完結**させる（`CLAUDE.md` 絶対ルール4）
- [ ] **2-2 実測の再現テスト**。実測4条件A〜Dを再現できることを自動チェックし、
      係数をいじってズレたらビルドで気づけるようにする
- [ ] **2-3 ボトルネック診断**。GPU律速／CPU律速／拮抗／ゲーム上限到達の判定と余力
- [ ] **2-4 交換先の提案**。DBの実在モデルで同じ式を再計算して
      「このGPUにすると X → Y」を出す。価格を持っていないので費用対効果には触れない
- [ ] **2-5 VRAM判定**。実測の4K高=8.19GBを根拠に、8GB以下＋4K高で警告。
      平均fpsではなく1% Low（カクつき）に効く点を添える
- [ ] **2-6 任意入力**（メモリ容量・電源W数）。**fps推定には使わない**と明記した上で、
      メモリは注意喚起、電源はGPU+CPUの公称TDP合計との比較のみ
- [ ] **2-7 解説文**。ルールベースで組み立てる。LLMは使わない

### 完成の判定基準

- GPUとCPUを選ぶだけで、推定fpsとボトルネック判定が出る
- **実測4条件を再現する**（4K高→464.6 / 1440p高→868.8 / 4K低→807.5 前後、
  1080p低→CPU天井970.3で頭打ち＋CPU律速と判定）
- 全ての数値に推定である旨と誤差、1080pには外挿である旨が出る
- Apexを選ぶと300fps上限の説明が出て、fps数値は出ない
- 診断1回あたりのネットワークリクエストとAI推論がゼロ
- 出さない値（1% Low・高負荷時）について、理由が画面に書かれている

### 依存関係

**フェーズ1**（スペックDBのデータ層）。投稿機能には依存しない
（投稿が集まったら精度を上げる形で後から効いてくる）。

### 想定される難所

- **1080pの係数が実測ではない**。最も使われる解像度なのに、実測の1080pは
  CPU律速でGPU側の係数が取れていない。実測1.87と画素比から指数則で
  外挿している（`CONTEXT.md`「1080pの係数」参照）。**ここが一番弱い**
- 設定係数1.74は4Kでの実測値で、他解像度でも同じとみなしている
- **対応ゲームを増やすには毎回実測が要る**。Apexは300上限で測定不能
- VRAM判定は検証機（16GB）では再現できず、投稿頼み
- 交換先の提案は、指数が数%しか違わないモデルを勧めると誤りになる。
  近接モデルの順位は信用できないため、意味のある差がある候補だけ出す

## フェーズ3 — ユーザー投稿機能

訪問者が自分の環境の実測fpsを投稿できる仕組み。
設計要件は `CONTEXT.md`「ユーザー投稿機能の設計要件」に記載済み。

### なぜ必要か

- **このサイト唯一の模倣不可能な資産**。他社ベンチの転載は禁止しているため
  （`CLAUDE.md` 絶対ルール1）、データを増やす手段は自前の実測か投稿しかない
- 公開ベンチが手薄な「設定別fps」「1% Low」「終盤の高負荷時」こそ価値がある
- **16GBの検証機では8GBカードのカクつきを再現できない**。
  この領域は投稿でしか埋まらない
- フェーズ4の精度がこのデータ量に直結する

### 実装単位

- [ ] **3-1 Supabase セットアップとスキーマ設計**
      （スキーマ＝どの項目をどんな型で保存するかの設計）
- [ ] **3-2 投稿フォーム**。必須項目は `CONTEXT.md` の設計要件をそのまま採る:
  - CapFrameX の **Remove outliers を ON** にした値
    （指定しないと投稿者ごとに 0.1% Low が10倍以上ばらつく）
  - 計測前に**5分程度のウォームアップ**を指示
    （冷えた状態はブーストが高く出る。起動直後はシェーダーコンパイルで
    巨大なスパイクが出て 0.1% Low が壊れる）
  - GPU使用率ではなく **GPU Limit Time** で律速を判定
    （使用率97%でも GPU Limit Time 85% というケースがある）
  - 解像度・画質設定・アップスケーリングの有無・ドライバ版・
    Resizable BAR / HAGS の状態
- [ ] **3-3 バリデーションとスパム対策**
- [ ] **3-4 モデレーション**。外れ値の検出と承認フロー
- [ ] **3-5 集計値の表示**。投稿件数と中央値を該当モデルのページに出す

### 完成の判定基準

- 投稿が保存され、**承認したものだけ**が表示される
- `CONTEXT.md` の必須項目が全てフォームにあり、**各項目になぜ必要かの説明が付く**
  （理由が無いと投稿者が条件を守らず、条件の違うデータが混入する）
- 自分の実測4点（条件A〜D）を投稿フォームから入力でき、
  `fps_measurement_sheet.csv` と同じ内容が保存される
- 明らかな外れ値が承認前に検出される

### 依存関係

**フェーズ1**（投稿を集めるには流入が要る）。
フェーズ2があると投稿の導線を各モデルページに置けるため、実際にはフェーズ2の後が望ましい。

### 想定される難所

- **鶏卵問題**。投稿が集まるまで機能に価値が無く、価値が無いと投稿されない。
  初期は自分の実測を見せて「こういうデータが欲しい」と示す必要がある
- **条件の違うデータの混入**。テストシーン・ドライバ・組み合わせるパーツが
  違うデータを混ぜると指数がノイズだらけになる（`CLAUDE.md` 絶対ルール1の理由2と同じ）
- 投稿の手間が大きい。CapFrameX の導入と設定を求めるため、
  記入項目を増やすほど投稿数は落ちる。精度と投稿数のトレードオフ
- 個人情報を扱わない設計にしておくこと

---

## フェーズ4 — 個別モデルページ 120本

GPU 78件・CPU 42件それぞれに独立したページを持たせる。
`CONTEXT.md`「次にやること」#2。

### なぜ必要か

- **SEOの本命**。「RTX 5070 スペック」等の検索を拾う入口が120本できる。
  一覧ページ1枚では拾える検索語が限られる
- ユーザー要望「個々のモデルが他とどれくらい差があるか分かること」の受け皿

### 実装単位

- [ ] **4-1 ルーティングと静的生成**（`/gpu/[slug]` / `/cpu/[slug]`）。
      120本をビルド時に生成する（静的生成＝アクセス時ではなくビルド時にHTMLを作る方式。
      表示が速く、サーバー費用もかからない）
- [ ] **4-2 差の可視化**。同世代・近接モデルとの比較
- [ ] **4-3 固有情報の追加**（薄いコンテンツ対策）。
      VRAM容量の判定、アーキ表記の注意、デュアルCCDの扱いなど、
      `CONTEXT.md` にある知見をモデルごとに出し分ける
- [ ] **4-4 メタデータ・構造化データ・sitemap**
      （構造化データ＝検索エンジンにページの内容を機械的に伝える記述）

### 完成の判定基準

- 120本すべてが個別URLで生成され、ビルド成果物に120個のHTMLがある
- 各ページに近接モデルとの比較と、そのモデル固有の解説がある
- 「近接モデルの順位は信用できない」旨が各ページに出る
- sitemap に120本が載る
- スペック表だけのページが1本も無い

### 依存関係

**フェーズ1**（データ層・指数の結合をそのまま使う）。

### 想定される難所

- **薄いコンテンツ判定のリスク**。スペック表を並べ替えただけの120本は
  検索エンジンに低品質と判断されうる。1本ごとに固有の中身が要る
- **その固有情報として最も欲しい「順位」が使えない**。指数の限界により
  近接モデルの優劣を断定できない。この2つの制約が同時に効くのが難所
- 120本分の文章をLLMで生成すると `CLAUDE.md` 絶対ルール4（AI推論を多用しない）に
  抵触する。生成するならビルド時に1回だけ行い、成果物をリポジトリに持つ

---

## フェーズ5 — 記事と収益化

### なぜ必要か

BTOパソコン・グラフィックボード・光回線のアフィリエイトと広告。
`CONTEXT.md`「プロジェクトの狙い」に、診断結果の出口がそのまま案件になる導線を
前提に設計すると記載がある。

### 実装単位

- [ ] **5-1 広告枠の設置**
- [ ] **5-2 アフィリエイト導線**。診断結果から条件に合う構成へ繋ぐ
- [ ] **5-3 計測**。どの導線が機能しているかを追える状態にする

### 完成の判定基準

- 診断結果から案件へ繋がる導線があり、クリックが計測できる
- 広告が一覧・個別ページの回遊を妨げていない
- 広告・アフィリエイトである旨の表示が適切に出ている

### 依存関係

**フェーズ1〜2**（流入）と**フェーズ4**（診断結果の出口）。

### 想定される難所

- **ASPの審査に一定のPVが必要**なことが多く、流入が立つまで着手できない
- 広告を早期に入れすぎると回遊とSEO評価を落とす。
  一覧・個別ページは検索流入の受け皿なので、ここの体験を優先する
- 診断結果と案件の対応付け。無関係な商品を出すと信用を落とし、
  「推定値であることを明示する」方針と相性が悪い

---

## 未決事項

決まっていない。決定するまで実装で前提にしないこと。

### 1. 対応ゲームの範囲

- 実測完了は **Valorant のみ**（4条件）
- **Apex Legends は300fps上限のため測定不能**。エンジン仕様で解除できず、
  検証機の構成では1440p最高でも4K最低でも300に張り付く
- ゲームを増やすたびに解像度係数・設定係数の再取得が必要
- 追加時は毎回「そのゲーム固有のfps上限」の有無を確認すること

判断に必要なもの: 何タイトルを対象にするか、Apex のように測定できない
タイトルをどう扱うか（上限だけ表示して予想は出さない等）。

### 2. ドメイン名

未定。

### 3. 公開の順番（細部）

**大方針は `CONTEXT.md`「公開の順番についての判断」で決定済み**
（一覧ページ先行 → 投稿収集 → 予想ツール）。ここを覆す話ではない。

未決なのは細部:

- 各フェーズをどの時点で公開するか
- 投稿機能を一覧ページの公開と同時に出すか、個別ページまで揃えてから出すか
- 予想ツールを、投稿が何件集まった時点で公開とするか
- フェーズ2の120本を一度に公開するか、段階的に増やすか

### 4. 使用するASPと具体的な案件

未定。BTOパソコン・グラフィックボード・光回線の3カテゴリを想定。

判断に必要なもの: 審査条件（必要PV）、報酬形態、フェーズ5の着手時期。

### 追加で洗い出した未決事項

整理の過程で出てきたもの。上の4つと同様に決定待ち。

- **「高負荷時の平均fps」の定義と測定方法**。現在の120秒キャプチャには
  該当する区分が無い。フェーズ4の出力3値のうち1つがこれに依存する
- **画質設定をどこまで実測で裏付けるか**。個別項目の係数を実測で取りに行くか、
  当面は段階プリセットのみに限定するか
- **Intel Arc を扱うか**。現在データ未収録。合成ベンチほど実ゲーム性能が出ない傾向があり、
  現在の指数の方式では特に外れやすい
